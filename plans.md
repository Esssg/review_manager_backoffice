# 사진 미리보기 개편 및 버전별 계정 튜토리얼 계획

- 작성일: 2026-08-14
- 상태: 최종안 확정
- 이번 작업에서 수정하는 파일: plans.md만
- 코드·DB·마이그레이션·버전 파일은 구현 단계에서 수정

## 1. 목표

이번 작업은 두 가지 범위를 함께 다룬다.

1. 사진 미리보기 모달을 운영자가 사진과 행 정보를 함께 확인하고, 현재 화면의 다른 행 사진으로 이동할 수 있는 구조로 개편한다.
2. 앱 버전을 0.0.0 형식으로 관리하고, 버전별 튜토리얼 완료 여부를 계정 단위로 기록해 새로고침·로그인 시 미완료 튜토리얼을 안내한다.

요청한 버전 기준은 다음처럼 기록한다.

- 1.0.0: 이번 기능 구현 전의 기준 버전
- 1.0.1: 사진 미리보기 개편 및 해당 튜토리얼이 포함된 버전

현재 package.json의 버전은 0.1.0이므로, 구현 시 버전 기준 파일과 실제 배포 버전을 함께 정리한다.

## 2. 현재 구조와 확인된 원인

### 사진 미리보기

- 공통 모달은 src/components/admin/product-detail/PhotoViewerModal.tsx다.
- 상품전체보기, 상품 상세, 리뷰받기 상세, 공개 리뷰받기 페이지가 이 모달을 공유한다.
- 상품전체보기 행에는 이미 업체명·품명·옵션·리뷰형태·구매자·수취인·구매계정과 review_photos가 함께 있다.
- 현재 사진 클릭 callback은 사진 배열과 사진 index만 전달하므로, 현재 행 정보와 다음 행 탐색 기준을 모달이 알 수 없다.
- 현재 사진 삭제 확인은 AppAlertDialog로 열리지만, 사진 모달 backdrop은 z-index 1300이고 AlertDialog 기본 overlay/content는 z-index 50이라 삭제 확인창이 사진 모달 뒤에 깔릴 가능성이 있다.

### 버전과 튜토리얼

- 현재 앱 버전·버전 이력·튜토리얼 완료 상태를 관리하는 코드나 DB 컬럼은 없다.
- 계정 정보는 허용 범위의 public.admins 테이블에서 관리한다.
- 현재 허용된 테이블 목록에는 별도 튜토리얼 테이블이 없다. 답변에서는 별도 튜토리얼 테이블을 선택했으므로, 구현 전 DB 대상 범위 확인이 필요하다.
- 튜토리얼 단계는 상품전체보기 메뉴, 전체보기, 사진 필터, 사진 모달을 실제 화면에서 순서대로 유도해야 한다.

## 3. 구현 계획 A — 사진 미리보기

### A-1. 삭제 확인창이 사진 모달 뒤에 뜨는 문제 수정

1. 사진 모달이 열린 상태에서 삭제를 누르면 확인창이 항상 최상위 레이어에 표시되도록 공통 overlay/content의 stacking order를 정리한다.
2. 사진 모달과 삭제 확인창의 포털 위치, overlay 순서, focus 이동을 확인한다.
3. 확인창에서 다음 동작을 보장한다.
   - 취소: 확인창만 닫히고 기존 사진 모달로 복귀
   - 삭제하기: 삭제 중 busy 상태를 표시하고 중복 입력 차단
   - Escape: 삭제 중이 아니면 확인창을 닫고, 삭제 중이면 닫히지 않음
   - 확인창이 사진 뒤에 깔리지 않아 취소·삭제 버튼을 모두 누를 수 있음
4. 삭제 성공·실패·이미 삭제된 사진의 화면 상태를 기존 삭제 서비스 계약에 맞춰 유지한다.
5. 삭제 성공 시 현재 navigation list에서 사진을 제거하고, 남은 사진이 있으면 가장 가까운 다음 사진을 표시한다. 남은 사진이 없으면 모달을 닫는다.

### A-2. 모달 레이아웃

상품전체보기에서 사용하는 PhotoViewerModal을 다음 2열 구조로 확장한다. 상품 상세·리뷰받기 상세·공개 리뷰받기의 기존 미리보기 UX는 이번 범위에서 변경하지 않는다.

- 왼쪽 절반: 리뷰어 공개 페이지의 정보 카드와 비슷한 label/value 형식
  - 업체명
  - 품명
  - 옵션
  - 리뷰형태
  - 구매자
  - 수취인
  - 구매계정
- 오른쪽 절반: 현재 사진을 object-fit contain으로 크게 표시
- 좁은 화면: 왼쪽 정보와 오른쪽 사진을 세로로 배치하거나 사진 우선으로 재배치
- 기존 하늘색 토큰·공통 Button·focus ring·위험 버튼 스타일을 재사용
- 사진 URL이 없거나 로드에 실패한 경우 빈 화면 대신 명확한 오류/대체 문구 표시

모달 action 영역은 다음 기능을 제공한다.

- 이전
- 다음
- 삭제(관리자 화면에서만)
- 닫기

### A-3. 현재 필터 결과 전체 기준 사진 이동

1. 상품전체보기 테이블에서 사진을 열 때 사진 배열만 전달하지 않고 현재 행 context까지 전달한다.
2. 현재 필터·상태의 전체 결과를 navigation 대상 범위로 사용한다. 아직 로드되지 않은 결과는 다음/이전 이동 시 필요한 페이지를 lazy fetch한다.
3. 현재 행의 여러 사진을 먼저 일렬로 이동한 뒤, 사진이 있는 다음/이전 행으로 넘어간다.
4. 사진이 없는 행은 탐색 대상에서 건너뛴다.
5. 첫 사진에서는 이전 버튼, 마지막 사진에서는 다음 버튼을 disabled 처리한다.
6. 삭제 후 현재 navigation list와 테이블 행의 사진 배열을 함께 갱신한다.

### A-4. 키보드와 접근성

- 상품전체보기 모달이 열린 동안 ArrowLeft는 이전, ArrowRight는 다음 사진으로 이동한다.
- Escape는 삭제 중이 아닐 때 모달을 닫는다.
- 모달 제목·현재 사진 설명·이전/다음/삭제/닫기 버튼의 aria label을 명확히 한다.
- 모달이 열릴 때 적절한 요소에 focus하고 닫을 때 사진을 열었던 썸네일로 focus를 복원한다.
- 삭제 확인창이 열린 동안에는 사진 모달의 키보드 조작이 실행되지 않도록 한다.
- 화면 폭이 좁아도 정보가 의도하지 않게 잘리지 않도록 overflow와 줄바꿈을 확인한다.

## 4. 구현 계획 B — 버전 기록

### B-1. 버전의 단일 기준

앱 버전 상수, package.json의 version, 사람이 읽는 버전 이력을 함께 관리한다.

- 실행 기준: src/constants/appVersion.ts 또는 동등한 공통 상수
- 배포 메타데이터: package.json의 version
- 이력 문서: docs/app_versions.md
- 관리자 사이드바 하단에 현재 앱 버전을 표시한다.

버전 판정은 공통 상수를 사용해 한 곳의 버전만 바꿔도 화면·튜토리얼 판정·문서 기록이 어긋나지 않도록 한다.

### B-2. 이번 버전 기록

- 1.0.0: 기존 상품전체보기·사진 미리보기 기능 기준점
- 1.0.1: 상품전체보기 사진 모달 2열 정보 표시, 삭제 확인창 레이어 수정, 현재 필터 결과 전체 기준 이전/다음 이동, 키보드 이동, 버전 튜토리얼 추가

### B-3. 이후 업데이트 규칙

향후 변경 때마다 다음 정보를 함께 기록한다.

- 버전 번호
- 변경일
- 변경 화면/기능
- 사용자에게 보이는 변경 내용
- 해당 버전에 튜토리얼이 있는지
- 튜토리얼 완료 판정 기준 버전

## 5. 구현 계획 C — 계정별 튜토리얼 상태

### C-1. 완료 상태 저장

계정별로 마지막으로 완료한 튜토리얼 버전을 저장한다.

답변 Q-11:C에 따라 별도 튜토리얼 진행 테이블을 사용한다. 이는 현재 AGENTS.md의 Supabase 허용 테이블 범위를 확장하는 결정이므로, 구현 단계에서 DB 대상 문서·마이그레이션·허용 범위를 함께 동기화한다.

별도 테이블을 승인하는 경우의 잠정 계약은 다음과 같다.

- 계정 식별자
- tutorial version
- 상태: skipped 또는 completed
- 완료/스킵 시각
- 계정·tutorial version 복합 unique

새 테이블을 허용하면 마이그레이션 적용, 최종 스키마 확인, docs/guide_db.md 갱신, 관련 테스트를 같은 작업에서 진행한다.

### C-2. 실행 시점

1. 관리자 로그인 직후 계정 프로필과 현재 앱 버전을 확인한다.
2. 새로고침으로 관리자 레이아웃이 다시 초기화될 때도 같은 판정을 수행한다.
3. 미완료 튜토리얼이 있으면 먼저 다음 안내 팝업을 표시한다.
   - 메시지: 새로운 버전으로 업데이트 됐습니다. 튜토리얼을 보시겠습니까?
   - 버튼: 스킵하기, 확인하기
4. 확인하기를 누르면 튜토리얼을 시작하고, 진행 중에는 일반 화면의 다른 행동을 막는다.
5. 튜토리얼 완료 시 계정의 해당 버전 상태를 completed로 저장한다.
6. 스킵하기를 누르면 해당 버전 상태를 skipped로 저장하고, 그 버전에 대해서는 다시 안내하지 않는다.

### C-3. 튜토리얼 정의와 실행기

튜토리얼을 버전별 단계 데이터로 분리한다. 현재 1.0.1에서는 최신 pending tutorial 하나만 안내하고, 추후 최초 접속자용 전체 튜토리얼 묶음을 순서대로 추가할 수 있게 만든다.

- 버전별 tutorial manifest
- 단계별 target selector 또는 ref
- 단계별 설명 문구
- 화살표 위치·반짝임·highlight 범위
- 허용되는 실제 interaction 종류
- 다음 단계 조건: 메뉴 click, filter selection, photo open, button click, keyboard event 등
- completed/skipped/replay 상태

예상 책임 분리:

- src/constants/*: 현재 앱 버전과 버전별 tutorial 정의
- src/services/*: 관리자 튜토리얼 완료 버전 조회·저장
- src/hooks/*: 로그인/새로고침 시 pending 판정 및 tutorial state machine
- src/components/common/*: 버전 안내 dialog, spotlight/backdrop, arrow/highlight, 완료 dialog
- AdminLayout 또는 관리자 capability/profile 경계: 관리자 전역 실행 시점 조합
- src/styles/*: 반짝임, spotlight, 화살표, overlay, 좁은 화면 스타일

## 6. 1.0.1 튜토리얼 단계 계획

튜토리얼 진행 중에는 spotlight 영역과 현재 단계에서 허용한 대상 외의 pointer·keyboard 행동을 막는다. 단, 실제 단계 진행에 필요한 메뉴·필터·사진·모달 버튼·좌우 화살표는 통과시킨다.

1. 상품전체보기 메뉴가 접혀 있으면 사이드바 펼치기 화살표를 highlight하고 반짝이게 표시한다.
   - 문구: 클릭하세요
2. 상품전체보기가 펼쳐지면 전체보기 메뉴를 highlight하고 반짝이게 표시한다.
   - 문구: 클릭하세요
3. 상품전체보기의 사진 필터에서 사진 있음을 선택하도록 유도한다.
4. 필터 결과의 사진 썸네일 하나를 클릭하도록 유도한다.
5. 모달 왼쪽 정보 영역을 highlight한다.
   - 업체명·품명·옵션·리뷰형태·구매자·수취인·구매계정 정보를 확인할 수 있다는 설명
6. 모달 오른쪽 사진 영역을 highlight한다.
   - 사진을 확인할 수 있다는 설명
7. 닫기 버튼을 highlight만 하고 모달은 유지한다.
   - 문구: 누르면 기존 화면으로 돌아가요~
8. 삭제 버튼을 highlight한다. 튜토리얼 모드의 삭제 click은 실제 삭제 요청 없이 다음 단계로만 진행한다.
   - 문구: 누르면 사진을 삭제할 수 있어요~
9. 다음 버튼을 highlight한다.
   - 문구: 누르면 다음 사진을 볼 수 있어요~
10. 이전 버튼을 highlight한다.
   - 문구: 누르면 이전 사진을 볼 수 있어요~
11. 좌우 화살표 키를 안내한다.
   - 문구: 키보드 좌, 우 화살표로도 넘길 수 있어요~
   - 좌 또는 우 화살표 입력으로 사진을 이동하면 튜토리얼을 종료한다.

사진이 없는 계정이나 필터 결과에서는 실제 DB를 변경하지 않는 메모리상의 tutorial demo 행·사진을 사용해 4~11단계를 진행한다. 실제 화면 click·filter·사진 열기 흐름은 유지하되, 삭제 동작은 tutorial mode에서 차단한다.

튜토리얼 종료 후에는 다음 dialog를 표시한다.

- 튜토리얼 다시보기: 1.0.1 단계를 처음부터 다시 실행
- 완료하기: 계정의 1.0.1 상태를 completed로 저장하고 종료

## 7. 삭제·실제 데이터 보호 계획

튜토리얼 8단계에서 실제 삭제가 일어나면 안 된다. 확정 정책은 다음과 같다.

- 실제 운영 사진이 있으면 삭제 버튼의 시각적 위치와 동작 설명만 확인하고 evidence_photos 삭제 요청은 보내지 않는다.
- 사진이 없는 계정은 메모리상의 demo 행·사진을 사용한다.
- 닫기 단계는 실제 click으로 모달을 닫지 않고 highlight만 진행해 삭제·이전·다음 안내가 끊기지 않게 한다.

실제 운영 사진을 튜토리얼 데이터로 삭제하는 방식은 사용하지 않는다.

## 8. 구현 순서

### Phase 0 — 질문 답변 및 기준선

- 확정 답변 반영 완료
- 현재 테스트·빌드·주요 사진 모달 동작 기준선 확인
- 1.0.0 기준점과 1.0.1 변경 범위 확정
- tutorial 저장 위치와 실제 interaction 정책 확정

### Phase 1 — 사진 모달 안정화

- 삭제 확인창 stacking/focus 수정
- 사진 모달 2열 layout 및 정보 card 추가
- 현재 행 context 전달
- 현재 필터 결과 전체 사진 navigation list 및 lazy fetch 구현
- 이전/다음 버튼 및 좌우 키보드 구현
- 상품전체보기 호출부만 개편하고 다른 공유 호출부는 기존 UX 유지

### Phase 2 — 버전 기준과 저장 계약

- 앱 버전 단일 기준 및 이력 문서 추가
- package.json 버전과 1.0.0/1.0.1 기록 정리
- 별도 튜토리얼 테이블 마이그레이션·서비스·문서 동기화

### Phase 3 — 튜토리얼 실행기

- pending tutorial 판정
- 새 버전 안내 dialog
- blocking spotlight/arrow/highlight
- 실제 click·select·photo open·button·keyboard 단계 전환
- destructive action 비파괴 처리
- 다시보기·완료하기 및 계정별 완료 저장

### Phase 4 — 검증

- 사진 삭제 확인창의 취소·확인·busy·Escape·focus 복귀
- 모달 2열 정보와 사진 표시
- 현재 필터 결과 전체 기준 이전/다음 및 좌우 키, 미로드 페이지 lazy fetch
- 첫 행·마지막 행 및 사진 없는 행 처리
- 로그인·새로고침·다른 계정·completed 계정·skipped 계정·미완료 계정
- 스킵(skipped)·다시보기·완료하기(completed)
- 튜토리얼 중 실제 데이터 삭제가 발생하지 않는지
- 데스크톱·좁은 화면·키보드 접근성
- npm test, npm run build, git diff --check
- Playwright MCP 또는 Chrome DevTools MCP 실제 브라우저 검증

## 9. 답변 반영 및 추가 확인

답변하신 내용을 다음과 같이 최종안에 반영했다. 기존 선택지는 답변 근거를 확인할 수 있도록 아래에 남겨두며, 추가 질문은 없다.

- Q-01 A: 상품전체보기만 새 2열 사진 모달 적용
- Q-02 A: 현재 행의 여러 사진을 포함한 일렬 navigation
- Q-03 B: 현재 필터 결과 전체를 대상으로 lazy fetch하며 이동
- Q-04 A: 첫/마지막 사진에서 해당 방향 버튼 disabled
- Q-05 B: 닫기 버튼 문구 유지
- Q-06 A: 실제 click·select·사진 열기·키 입력으로 튜토리얼 진행
- Q-07 A: 닫기 버튼은 highlight만 하고 모달 유지
- Q-08 A: 튜토리얼의 삭제 click은 실제 삭제 없이 시뮬레이션
- Q-09 A: 사진이 없으면 메모리상의 demo 행·사진 사용
- Q-10 B: 스킵도 해당 버전의 skipped 상태로 기록하고 재안내하지 않음
- Q-11 C: 별도 튜토리얼 테이블을 사용하고 DB 허용 범위·문서·마이그레이션을 함께 확장
- Q-12 A: 현재는 최신 tutorial 하나만 안내하고, 추후 최초 접속자용 전체 tutorial 묶음으로 확장
- Q-13 A: package.json·앱 버전 상수·docs/app_versions.md에 기록
- Q-14 A: SemVer 기준으로 patch/minor/major를 구분
- Q-15 B: 관리자 사이드바 하단에 현재 버전 표시
- Q-16 A: 로그인 후 상품전체보기 전체보기로 자동 이동해 시작

### 답변 원문 및 선택지

### Q-01. 새 2열 사진 모달을 적용할 범위는 어디까지인가요?

- A. 상품전체보기만 (추천) — 이번 튜토리얼과 요청 화면에 집중하고 다른 공유 호출부 회귀를 최소화한다.
- B. 관리자 사진 미리보기 전체 — 상품전체보기·상품 상세·리뷰받기 상세를 같은 모달 UX로 통일한다.
- C. 관리자와 공개 리뷰어 페이지 전체 — 공개 페이지도 동일한 정보/버튼 구조로 맞춘다.
Q-01: A

### Q-02. 이전/다음의 이동 단위는 어떻게 할까요?

- A. 현재 화면의 사진을 일렬로 이동 (추천) — 현재 행의 여러 사진을 먼저 이동한 뒤, 다음 사진이 있는 행으로 넘어간다.
- B. 항상 다음/이전 행의 첫 사진으로 이동 — 같은 행에 사진이 여러 장이어도 행 단위로 이동한다.
- C. 현재 행의 사진 안에서만 이동 — 다른 행으로 넘어가지 않는다.

Q-02:A

### Q-03. 현재 화면 기준의 범위는 어디까지인가요?

- A. 현재 필터·상태로 화면에 로드된 행만 (추천) — 현재 보고 있는 표의 행 순서와 일치하며, 사진 없는 행은 건너뛴다.
- B. 현재 필터 결과 전체 — 아직 로드되지 않은 다음 행도 추가 조회해 이동한다.
- C. 브라우저에 보이는 viewport 안의 행만 — 화면 아래로 내려간 행은 탐색하지 않는다.

Q-03: B

### Q-04. 이전/다음의 첫 행·마지막 행 동작은 어떻게 할까요?

- A. 해당 방향 버튼을 disabled 처리 (추천) — 현재 화면의 처음과 끝을 명확히 표시한다.
- B. 반대쪽 끝으로 순환 — 마지막에서 다음을 누르면 첫 사진으로 이동한다.

Q-04: A

### Q-05. 모달 닫기 버튼의 표시 문구는 무엇으로 할까요?

- A. 취소 (추천) — 요청한 튜토리얼 문구와 버튼 명칭을 일치시킨다.
- B. 닫기 — 기존 사진 모달 문구를 유지한다.

Q-05: B

### Q-06. 튜토리얼 단계는 실제 화면을 실제로 클릭하는 방식인가요?

- A. 실제 클릭·선택·사진 열기·키 입력으로 진행 (추천) — 사용자가 배운 동작을 그대로 수행한다.
- B. 화면은 highlight만 하고 별도 다음 버튼으로 진행 — 실제 데이터·화면 상태를 바꾸지 않아 안정적이다.

Q-06: A


### Q-07. 튜토리얼 7단계의 취소 버튼은 실제로 모달을 닫을까요?

- A. 버튼을 highlight만 하고 모달은 유지 (추천) — 8~10단계 버튼을 바로 이어서 안내할 수 있다.
- B. 실제로 닫은 뒤 같은 사진 모달을 자동으로 다시 열고 8단계로 진행 — 취소 동작까지 실제로 보여준다.
- C. 실제로 닫고 사진을 다시 누르는 단계를 추가 — 실제 흐름은 정확하지만 단계가 늘어난다.

Q-07: A

### Q-08. 튜토리얼 8단계 삭제 버튼은 어떻게 처리할까요?

- A. 삭제 요청 없이 튜토리얼 동작만 시뮬레이션 (추천) — 운영 사진을 절대 삭제하지 않는다.
- B. 별도 demo 데이터/사진을 준비해 demo에서만 실제 삭제 — 실제 버튼 흐름은 재현하지만 demo 데이터 관리가 필요하다.
- C. 현재 사진을 실제 삭제 — 운영 데이터가 삭제될 수 있어 권장하지 않는다.

Q-08: A

### Q-09. 사진이 없는 계정/필터 결과에서 튜토리얼을 어떻게 진행할까요?

- A. 메모리상의 demo 행·사진을 튜토리얼 전용으로 표시 (추천) — 모든 계정이 실제 DB 데이터 없이 같은 단계에 참여할 수 있다.
- B. 계정의 실제 사진을 사용하고 사진이 없으면 튜토리얼을 시작하지 않음 — 구현은 단순하지만 계정별 경험이 달라진다.
- C. 운영용 seed 사진을 별도로 준비 — 모든 계정에서 가능하지만 DB 샘플/권한 관리가 필요하다.

Q-09: A

### Q-10. 스킵하기를 누른 계정은 언제 다시 안내할까요?

- A. 이번 로그인/새로고침에서만 건너뛰고 다음 로그인·새로고침에 다시 안내 (추천) — 실제로 완료하지 않은 계정은 계속 배울 수 있다.
- B. 스킵도 완료로 기록하고 다시 안내하지 않음 — 사용자가 영구적으로 거부할 수 있다.
- C. 스킵 후 같은 세션에서는 다시 안내하지 않고, 다음 로그인 때만 안내 — 새로고침에는 다시 띄우지 않는다.

Q-10: B

### Q-11. 계정별 튜토리얼 완료 상태는 어디에 저장할까요?

- A. admins에 마지막 완료 튜토리얼 버전 컬럼 추가 (추천) — 계정 기준으로 기기·브라우저가 달라도 유지되며, 허용된 테이블 범위 안에서 처리할 수 있다.
- B. localStorage에 저장 — DB 변경은 없지만 브라우저·기기별로 완료 상태가 달라진다.
- C. 별도 튜토리얼 테이블 생성 — 이 저장소의 허용 테이블 범위 밖이므로 별도 승인이 필요하다.

Q-11: C

### Q-12. 이전 버전 튜토리얼을 여러 개 놓친 계정은 어떻게 할까요?

- A. 현재 최신 튜토리얼만 안내 (추천) — 예: 완료 버전이 1.0.5, 현재가 1.0.7이면 1.0.7만 진행한다.
- B. 놓친 버전을 순서대로 모두 안내 — 교육 누락은 줄지만 로그인 후 여러 튜토리얼을 연속 진행한다.
- C. 관리자가 튜토리얼별로 안내 여부를 선택 — 유연하지만 관리 화면과 별도 상태 구조가 필요하다.

Q-12: 일단은 A인데, 나중에는 최초접속자의 경우 전체 튜토리얼을 만들 계획임

### Q-13. 버전 이력은 어디에 남길까요?

- A. package.json + 앱 버전 상수 + docs/app_versions.md (추천) — 배포 메타데이터·실행 기준·사람이 읽는 이력을 모두 남긴다.
- B. package.json과 앱 버전 상수만 — 파일 수를 줄이지만 변경 설명 이력은 Git에 의존한다.
- C. package.json만 — 가장 단순하지만 튜토리얼 기준 버전과 이력 관리가 약해진다.

Q-13: A

### Q-14. 이후 SemVer 번호를 올리는 기준은 어떻게 할까요?

- A. 버그/UI 수정은 patch, 호환 가능한 기능 추가는 minor, breaking 변경은 major (추천)
- B. 매 업데이트마다 직접 번호 지정
- C. 모든 사용자 기능 변경은 minor, 버그 수정만 patch

Q-14: A

### Q-15. 앱 버전을 사용자 화면에도 표시할까요?

- A. 기록만 하고 화면에는 표시하지 않음 (추천) — 요청한 버전/튜토리얼 판정에 집중한다.
- B. 관리자 설정 또는 사이드바 하단에 현재 버전 표시
- C. 기록과 화면 표시 모두 적용

Q-15: B

### Q-16. 로그인 후 다른 관리자 화면에 있을 때 튜토리얼을 어디서 시작할까요?

- A. 상품전체보기 전체보기로 자동 이동 후 시작 (추천) — 1~4단계 target을 항상 찾을 수 있다.
- B. 현재 화면을 유지하고 상품전체보기로 이동할 때 시작 — 사용자의 현재 작업을 방해하지 않는다.
- C. 현재 화면 위에 안내만 띄우고 사용자가 직접 상품전체보기로 이동 — 자동 라우팅은 없지만 튜토리얼 흐름이 끊길 수 있다.

Q-16: A

## 10. 구현 시작 조건

다음 항목이 확정되면 코드 구현을 시작한다.

- Q-01~Q-05: 사진 모달 범위·탐색·버튼 계약 확정
- Q-06~Q-10: 튜토리얼 실제 interaction·비파괴 정책·스킵 정책 확정
- Q-11~Q-15: 계정 저장·누락 버전·버전 기록·표시 정책 확정
- Q-16: 로그인/새로고침 시작 라우트 확정
- 1.0.0 기준점과 1.0.1 배포 버전 확정

위 결정사항을 기준으로 사진 모달, DB, 버전 파일, 튜토리얼 구현을 시작한다.
