# 회사·계정·권한·페이지 설정 개편 계획

- 작성일: 2026-08-28
- 상태: Q1~Q50 답변 반영 완료 · 운영 호환성 원칙·코드 구현 단계 반영 완료
- 현재 구현 단계의 변경 범위: 로컬 소스 코드·테스트·명시적 migration/Edge Function 초안 작성과 검증, 사용자 승인 후 staging migration/RPC·`admin-gateway` Edge Function·backoffice 웹 이미지 적용·검증까지 수행한다. 2026-08-29에는 production bundle 계약 후속 RPC와 해당 웹 수정도 승인 후 별도 반영했다.
- 이번 단계에서 하지 않는 것: production 전체 C4 migration 재실행, restrictive RLS 적용, 공개 사진 흐름 전환, 승인 없는 운영 데이터 변경. `admin-gateway` Edge Function nested action path 1·2차 호환 수정과 bundle 계약 후속 RPC, app-only 웹 이미지 배포는 각각 별도 대상·승인으로 수행했다.
- 현재 로컬 구현 진행: 권한/설정 도메인 계약·resolver, 호환 access bundle, gateway 세션/설정/임직원 권한 초안, 설정·권한 관리 화면, 상품 기본값·예정 입금자명 유틸 통합을 반영했다. C4 로컬 단계에서는 관리자 데이터 서비스 전체에 gateway-first adapter·operation allowlist·client identity 제거·legacy fallback을 연결했고, 주요 화면 action 권한 검사와 gateway 서버-side operation 권한 재검사 초안, Q49 메뉴·capability 기반 resource-action binding backfill 초안을 추가했다. staging `vm-app-01`에 다섯 migration과 `admin-gateway` 함수를 적용하고 RPC/read/세션 경계와 역할별 scope를 검증했으며, `vm-web-01`에는 same-origin Nginx proxy를 포함한 새 backoffice 이미지를 배포하고 health/proxy smoke를 통과시켰다. `hyejin2054`의 staging 인증 read/write canary에서 대시보드·리뷰받기·상품전체보기·일괄수정·전체/사진 내보내기와 상품·제출 mutation, 직접 URL 권한 fallback을 확인했고, 브라우저 관리자 데이터 요청은 gateway-only였다. 기존 관리자 데이터 서비스의 legacy fallback은 rollback window 동안 유지한다.
- 현재 로컬 범위에서 developer 전체 회사 scope와 company_admin 동일 회사 employee scope를 resolver·서비스 계약·임직원 대상 목록에 반영했다. 저장소 기본값은 `READY=false`로 유지하지만, 승인된 staging `vm-web-01`의 현재 canary 이미지는 `VITE_ADMIN_GATEWAY_READY=true`로 인증 read/write 전환되어 있다. staging 쓰기 canary는 생성한 상품·제출·사진 row가 모두 정리된 상태로 완료했으며, 공개 사진 흐름·RLS·운영 전환은 아직 수행하지 않았다.
- gateway 활성화 안전장치: `VITE_ADMIN_GATEWAY_ENABLED=true`, URL 설정, 전체 관리자 데이터 서비스 전환과 staging/canary 검증을 모두 마친 뒤 `VITE_ADMIN_GATEWAY_READY=true`를 별도로 설정해야 한다. 세 조건 중 하나라도 없으면 기존 직접 Supabase 호환 경로만 사용한다. production Edge Function에는 nested action path 1·2차 수정이 적용됐고, bundle 계약 후속 RPC와 app-only 웹 이미지도 반영됐지만 production 전체 C4 전환·RLS는 미완료다. 실제 production permission write와 일괄입력 저장은 별도 브라우저 세션 검증이 필요하다.
- 2026-08-29 `vm-app-01` production DB 사전 점검에서는 gateway 핵심 테이블/RPC와 `permission_bindings=206`이 이미 확인되어 문서의 production 미적용 전제와 불일치했다. 다섯 migration은 재실행하지 않았고, 승인된 `20260829220000_fix_admin_review_receive_product_bundle_contract.sql`만 DB custom dump/RPC 원본 백업 후 적용했다.

## 0. 이 계획의 사용 방법

이 문서는 회사·관리자·임직원 계층과 DB 기반 설정을 구현하기 위한 기준안이다. Q1~Q50 답변이 모두 반영되었으므로 이제 코드 구현의 기준으로 사용한다. 운영 DB 적용과 서버 배포는 별도의 승인 절차를 거친다.

답변은 아래처럼 보내면 된다.

```text
Q1=A
Q2=A, Q3=B
Q4=개인 거부 우선
...
```

선택지에 없는 요구사항은 질문 번호 뒤에 원하는 규칙을 직접 적어도 된다.

### 운영 DB 변경 허용 및 운영 웹 무중단 호환 원칙 (최우선)

운영 DB의 테이블·컬럼·인덱스·RPC·RLS 변경은 허용한다. 단, **변경으로 현재 운영 웹서버의 로그인, 메뉴, 조회, 생성, 수정, 삭제, 파일 업로드, 내보내기, 일괄수정, 공개 리뷰받기 동작에 이상이 생기는 것은 절대 허용하지 않는다.** 이 원칙은 기능 추가보다 우선하며, 조건을 만족하지 못하는 migration·코드 배포는 중단한다.

1. 기존 운영 웹 번들이 동작하는 상태에서 schema를 먼저 additive하게 추가한다. 기존 컬럼·RPC·응답 형식은 즉시 삭제·이름변경하지 않고, 새 컬럼은 nullable 또는 기존 동작과 같은 default로 추가한다.
2. backfill은 멱등·분할 처리하고, 적용 전후 row count·핵심 샘플·제약·인덱스·RPC 결과를 비교한다. 실패한 batch와 완료 위치를 기록해 재실행할 수 있어야 한다.
3. 새 코드가 준비되기 전에는 기존 웹 번들이 사용하던 직접 조회·쓰기 경로와 메뉴 권한을 그대로 유지한다. 새 구조가 없어도 기존 화면이 빈 화면·컬럼 오류·권한 오류를 내지 않아야 한다.
4. 새 서버 게이트웨이와 새 웹 번들을 staging 및 개발자/테스트 계정으로 검증한 뒤 canary 전환한다. 모든 활성 클라이언트가 새 게이트웨이 계약으로 이동하고 기존 번들 갱신 안내가 끝나기 전에는 restrictive RLS를 켜지 않는다.
5. RLS/RPC 활성화 후 구형 번들로 되돌리면 기능이 깨질 수 있으므로, 구형 번들 rollback window가 끝난 뒤에만 정책을 강화한다. 그 전까지는 호환 RPC·정책을 유지하고, 강화 시점에는 이전 번들의 직접 접근을 안전한 업데이트 안내로 종료한다.
6. 공개 `/review-receive/*`의 anon 조회·사진 동기화 계약은 관리자 권한 개편과 분리해 유지한다. 공개 읽기 정책·기존 `review-receive-photo-sync` 흐름을 별도로 smoke test하지 못하면 관리자 RLS를 활성화하지 않는다.
7. destructive migration, 기존 RPC 시그니처 변경, 기존 응답 필드 제거, 기존 데이터 일괄 재계산은 별도 승인·백업·검증 없이는 실행하지 않는다. 운영 장애가 감지되면 즉시 웹 배포를 멈추고 마지막 호환 버전으로 되돌릴 수 있어야 한다.

운영 전환 순서는 다음을 따른다.

| 순서 | DB/서버 상태 | 운영 웹 상태 | 다음 단계 조건 |
|---|---|---|---|
| 1 | 기존 schema·RPC 유지 | 기존 번들 정상 동작 | baseline smoke/build 통과 |
| 2 | 새 테이블·컬럼·호환 RPC additive 추가 | 기존 번들 계속 정상 동작 | schema·row count·RPC 비교 통과 |
| 3 | 서버 게이트웨이·단기 세션 준비 | 기존 번들과 병행 가능 | 인증·권한·오류 계약 통과 |
| 4 | 새 권한/설정 read·write 경로 준비 | 개발자·테스트 canary | 기존 기능 회귀 없음 |
| 5 | 새 번들 전체 전환, 구형 번들 업데이트 유도 | 새 번들 정상 동작 | rollback window 종료·구형 클라이언트 차단 |
| 6 | restrictive RLS/RPC 정책 활성화 | 게이트웨이 경유만 허용 | 직접 테이블 접근·권한 우회 없음 |
| 7 | 구형 호환 계층 정리 | 새 계약만 사용 | 별도 cleanup 승인·문서 동기화 |

## 1. 현재 기준선

### 1-1. 현재 계정·회사 구성

현재 운영 DB에서 확인한 계정은 다음과 같다.

| 계정 | 현재 회사 | 이번 개편에서 확정된 초기 역할 |
|---|---|---|
| `2sssg` | 한화시스템 | `developer` |
| `hyejin2054` | 시나브로 | 회사 관리자 |
| `aram2525` | 시나브로 | 임직원 |
| `kimhanbi77` | 시나브로 | 임직원 |
| `tjsal9601` | 시나브로 | 임직원 |
| `test1` | 테스트커머스 | `company_admin` |
| `test2` | 테스트커머스 | `company_admin` |
| `test3` | 테스트커머스 | `company_admin` |

사용자가 지정한 기준은 다음과 같이 반영한다.

- 시나브로: `hyejin2054`는 관리자, 나머지는 임직원
- 한화시스템·테스트커머스: 관리자급으로 취급하되, Q2 답변에 따라 `2sssg`만 전역 `developer`, 테스트 계정은 테스트커머스 `company_admin`으로 시작한다.
- 한 회사에 `company_admin`을 여러 명 둘 수 있고(Q3), 임직원은 회사 소속만으로 연결한다(Q4).
- 계정 생성·비활성화·회사 이동·역할 변경은 백오피스가 아니라 외부 운영 절차로 처리한다(Q21).

### 1-2. 현재 메뉴 권한

현재 `admin_menu_permissions`의 메뉴 번호는 아래와 같다.

- `1`: 대시보드
- `2`: 상품
- `3`: 리뷰받기
- `4`: 상품전체보기
- `5`: 내보내기
- `6`: 파일 업로드
- `7`: 일괄수정하기

운영 DB의 현재 권한은 다음과 같다.

- `2sssg`: `1,2,3,4,5,6,7`
- `aram2525`, `hyejin2054`, `kimhanbi77`, `test1`, `tjsal9601`: `1,3,4,5,7`
- `test2`, `test3`: `1,3,4,5`

이 권한 행은 구현 시 개인 권한 또는 회사·역할 기본 권한으로 이관할 수 있어야 한다. 기존 권한을 임의로 잃게 만들지 않는 것이 마이그레이션의 기본 원칙이다.

### 1-3. 현재 구현에서 확인된 계정별 값

- `admins` 행에 로그인 ID, 비밀번호, 이름, 연락처, 이메일, 회사가 있다.
- `admin_menu_permissions`가 계정별 메뉴 노출과 직접 URL 접근을 결정한다.
- `admin_tutorial_progress`가 계정·튜토리얼 버전별 `skipped/completed` 상태를 저장한다.
- 회사 데이터 포함 기본값과 입금확인 권한은 설계상 `admins` 컬럼으로 이동되어 있으나, 현재 운영 DB에는 두 컬럼이 없어 `adminCapabilities.ts`의 계정 ID 하드코딩 fallback이 실행 중이다.
- 상품 소유 범위는 `products.manager_id -> admins.login_id` 연결로 계산한다.
- 로그인 ID, 사이드바 접힘, 내보내기 컬럼, 리뷰받기 필터 일부는 브라우저 저장값으로 관리된다. 이 중 일부는 계정 ID가 key에 포함되지 않아 계정 간 공유된다.

### 1-4. 현재 DB 대상 범위 제약

이 저장소가 변경할 수 있는 `public` 테이블은 현재 다음 8개로 한정되어 있다.

`admins`, `products`, `admin_menu_permissions`, `product_steps`, `applications`, `submissions`, `evidence_photos`, `admin_tutorial_progress`

새로운 회사·권한·설정 테이블을 추가하는 안은 현재 범위를 벗어나므로 Q1 답변으로 허용했다. Q27·Q38·Q49·Q50 답변까지 반영해 테이블·권한·호환·서버 경계를 정했지만, 실제 migration은 구현 단계에서 별도 승인 후 실행한다. 어떤 안을 선택하더라도 적용 후 `docs/guide_db.md`와 실제 스키마를 함께 동기화한다.

## 2. 목표

1. 회사마다 관리자와 임직원을 구분한다.
2. 개발자는 모든 회사의 계정·역할·권한을 관리할 수 있다.
3. 회사 관리자는 같은 회사 임직원의 권한과 회사 기본값을 관리할 수 있다.
4. 임직원은 자신의 웹 화면 설정과 상품 생성 기본값을 저장할 수 있다.
5. 메뉴 권한과 입금확인 같은 동작 권한을 코드의 계정 ID 하드코딩이 아니라 DB에서 결정한다.
6. 제품비·리뷰비 입금구분 같은 기본값을 생성 화면에 자동 적용한다.
7. 예정 입금자명 회사명 자르기 규칙을 DB 설정으로 관리한다.
8. 메뉴를 숨기는 것뿐 아니라 직접 URL, 화면 액션, 서버 쓰기 작업도 같은 권한 계약으로 보호한다.
9. 기존 운영 데이터와 현재 권한을 보존하면서 단계적으로 전환한다.

## 3. 제안하는 목표 모델

아래 모델을 기본안으로 삼고, 질문 답변에 따라 세부 계약을 확정한다.

### 3-1. 역할과 계층

#### `developer`

- 모든 회사의 계정·회사·역할·권한을 조회하고 설정한다.
- 모든 메뉴와 허용된 운영 액션을 기본적으로 사용할 수 있다.
- 특정 회사의 회사 관리자나 임직원 권한을 회사 범위 제한 없이 관리한다.

#### `company_admin`

- 자신이 속한 회사의 employee 목록을 조회하고 권한을 설정한다.
- 한 회사에 여러 명이 있을 수 있으며, 같은 회사 관리자들은 같은 관리 범위를 갖는다.
- 회사 기본값과 회사 공통 예정 입금자명 규칙을 관리할 수 있다.
- 다른 회사 계정, 개발자 계정, 다른 company_admin의 권한, 역할·회사 이동은 변경할 수 없다.

#### `employee`

- 자신에게 부여된 메뉴·액션만 사용한다.
- 자신의 웹 화면 설정과 상품 생성 기본값을 관리한다.
- 다른 계정의 권한이나 회사 공통 설정은 변경할 수 없다.

역할은 화면 표시용 문구가 아니라 데이터 접근·설정 대상·쓰기 권한을 계산하는 도메인 값으로 관리한다. 초기 역할 배정과 회사 관리자 다중 허용은 Q2·Q3 답변을 반영했다. 임직원은 회사 소속만으로 연결하며 별도 상위 관리자 트리는 두지 않는다.

### 3-2. 권한 종류

메뉴 권한과 동작 권한을 분리한다.

- 메뉴: 대시보드, 상품, 리뷰받기, 상품전체보기, 내보내기, 파일 업로드, 일괄수정
- 조회 액션: 본인 데이터 조회, 회사 데이터 조회, 제출 조회, 사진 조회
- 쓰기 액션: 상품 생성·수정·삭제, 신청자 확정, 제출 생성·수정·삭제, 단계 변경
- 민감 액션: 입금완료 처리, 실제 입금자명 수정, 일괄수정 적용, 사진 삭제
- 관리 액션: 회사 임직원 목록 조회, 임직원 권한 변경, 회사 기본값 변경

`can_verify_deposit`처럼 특정 계정 ID를 코드에서 검사하는 방식은 제거하고, `submission.deposit.verify` 같은 권한 코드로 통일한다. 실제 이름은 Q5에서 확정한다.

### 3-3. 권한 적용 대상

같은 권한 코드를 다음 세 대상에 설정할 수 있게 한다.

1. 회사 기본 권한
2. 역할 기본 권한(예: 회사 내 임직원 공통)
3. 개인 예외 권한

개인에게 최종 적용되는 권한은 회사·역할·개인 규칙을 합쳐 계산한다. Q6 답변에 따라 더 구체적인 규칙이 우선하고, 같은 수준에서는 명시적 거부가 허용보다 우선한다. 권한 행이 없으면 Q7 답변에 따라 거부한다.

### 3-4. 설정 종류

#### 접근·권한 설정 — 관리자 영역

- 역할과 활성/비활성 상태
- 회사 소속과 상위 관리자
- 메뉴별 조회 권한
- 액션별 생성·수정·삭제·확인 권한
- 회사 데이터 조회 허용 여부
- 임직원 관리 가능 범위

#### 회사 기본값 — 회사 관리자 영역

- 회사명 자르기 글자 수
- 제품비 입금구분 기본값
- 리뷰비 입금구분 기본값
- 필요 시 리뷰비 기본 금액, 실배송 여부, 리뷰형태 등

#### 개인 기본값 — 임직원 영역

- 상품 생성 시 기본 입금구분
- 상품 생성 시 기본 리뷰형태·실배송 여부
- 상품 생성 폼의 초기값
- 리뷰받기 기본 탭·회사 범위
- 대시보드 초기 기간
- 내보내기 컬럼 선택
- 사이드바 접힘 상태

처음부터 모든 화면의 값을 무제한으로 저장하지 않고, 승인된 설정 키 목록과 타입·기본값·설명·적용 화면을 함께 관리한다. 추후 설정 항목을 추가할 수 있도록 확장하되 임의 JSON 값이 곧 권한이 되지 않게 분리한다.

### 3-5. 설정 상속 순서

확정된 기본 순서는 다음과 같다.

`회사 기본값 → 역할 기본값 → 개인 기본값 → 현재 입력값`

현재 입력값은 저장 전 미리보기와 해당 생성 작업에만 적용한다. 기존 상품의 저장값을 자동으로 덮어쓰지 않는다. 회사 기본값은 company_admin/developer가 즉시 저장할 수 있고, 새 상품부터 적용한다. 개인 override는 Q33·Q48 답변에 따라 유지하고 employee가 해제할 수 있으며, developer만 감사 이력을 남기고 긴급 초기화할 수 있다.

### 3-6. 예정 입금자명 규칙

현재 공통 유틸인 `src/utils/plannedDepositorName.ts`의 날짜 포맷 흐름은 재사용하되, 회사명 자르기 길이는 DB 설정에서 읽는다.

기본 계산식:

```text
MMDD + 회사명 앞 N글자
```

- `N = 0`: 회사명 전체 표시
- `N > 회사명 길이`: 전체 표시
- 예: 날짜가 0401이고 회사명이 한화시스템, `N = 3`이면 `0401한화시`
- 제품비·리뷰비가 모두 업체 부담인 현재 `업체페이` 규칙은 별도의 고정 업무 규칙으로 둘지 설정값으로 옮길지 Q24에서 결정한다.

이 규칙은 상품/품목 생성·수정, 리뷰받기 일괄 입력, 파일 업로드 등 모든 진입점에서 같은 순수 유틸을 사용해야 한다. 설정 변경으로 이미 저장된 `planned_depositor_name`을 자동 변경할지는 Q16에서 확정한다.

## 4. DB 설계 선택지와 현재 결정

### 안 A — 현재 허용된 8개 테이블 안에서 확장

현 저장소 범위를 유지하면서 다음처럼 설계한다.

- `admins`: 역할, 활성 상태, 상위 관리자 연결, 회사/역할/개인 기본값 컬럼 또는 승인된 설정 JSON을 추가한다.
- `admin_menu_permissions`: 기존 개인 메뉴 행을 보존하면서 대상 종류(회사·역할·개인), 권한 코드, 허용/거부 효과를 표현하도록 확장한다. 기존 `menu_number`는 호환 필드로 유지하거나 단계적으로 대체한다.
- `products`: 기존 상품 데이터와 생성 시 적용된 기본값 결과를 유지한다. 상품 담당자 연결은 기존 `manager_id`를 유지한다.
- `admin_tutorial_progress`: 기존 계정·버전별 상태를 유지한다.

장점은 현재 DB 대상 범위를 늘리지 않는다는 점이다. 단점은 `admin_menu_permissions`가 메뉴와 액션 권한을 함께 담게 되고, 회사 기본값을 어느 계정 행에 둘지 추가 규칙이 필요하다는 점이다.

### 안 B — 권한·회사·설정을 정규화한 별도 테이블 추가 (선택됨)

회사, 역할, 권한 정의, 권한 부여, 설정값을 각각 정규화한다. 권한 부여에는 대상 종류·대상 ID·권한 코드·효과·적용 범위를 둔다.

장점은 회사/역할/개인 권한과 미래 설정을 명확히 분리하고 다중 관리자·감사 이력을 확장하기 쉽다는 점이다. 단점은 현재 AGENTS.md의 DB 허용 목록을 확장하는 사용자 승인, 마이그레이션, 문서 동기화가 필요하다는 점이다.

Q1 답변으로 안 B를 선택했다. 따라서 구현 전에 다음을 지킨다.

1. 새 테이블의 정확한 목록과 각 테이블의 책임을 먼저 확정한다.
2. 현재 AGENTS.md의 DB 허용 목록과 `docs/guide_db.md`를 새 테이블까지 포함하도록 승인·갱신한다.
3. 운영 DB에 바로 적용하더라도 기존 클라이언트와 데이터가 깨지지 않도록 additive migration → backfill → dual-read/dual-write 또는 호환 view/RPC → 전환 → 충분한 검증 후 정리 순서를 사용한다.
4. 기존 테이블·컬럼을 삭제하거나 이름을 바꾸는 작업은 별도의 호환성 확인과 롤백 기준 없이는 진행하지 않는다.
5. Q27·Q38·Q49·Q50에서 정한 테이블 목록, 권한표, 호환성, 서버 경계를 기준으로 migration을 설계한다. 실제 적용은 구현 단계의 별도 승인과 staging 검증 후에만 실행한다.

### RLS 적용 순서에 대한 필수 호환성 규칙

현재 웹 번들은 Supabase Auth 세션이 아니라 `localStorage`의 `review_manager_admin_id`를 보내는 구조다. 따라서 새 RLS를 기존 웹 번들보다 먼저 활성화하면 다음 문제가 생길 수 있다.

- `auth.uid()` 또는 새 권한 claim을 요구하는 정책은 기존 요청을 계정 요청으로 인정하지 못해 SELECT가 빈 결과가 되거나 INSERT/UPDATE/DELETE가 권한 오류로 실패할 수 있다.
- `p_admin_id` 같은 요청 파라미터만 믿는 호환 정책은 ID를 위조할 수 있어 보안 해결책이 아니다.
- 새 테이블만 먼저 RLS를 켜는 경우에도, 새 번들이 아직 해당 테이블을 사용하지 않으면 기존 화면의 권한·설정 조회 흐름이 깨질 수 있다.

따라서 RLS를 선택하는 경우의 기본 배포 순서는 다음으로 고정한다.

1. 기존 테이블에 대한 restrictive RLS를 먼저 켜지 않고, 새 테이블·컬럼·RPC를 additive하게 준비한다.
2. 새 웹 코드가 새 인증/권한 계약을 읽고 쓸 수 있도록 배포하고, 이전 클라이언트와의 호환 동작을 확인한다.
3. 실제 인증 주체를 식별할 수 있는 세션/서버 검증과 정책을 함께 준비한다.
4. 새 클라이언트가 안정적으로 동작하는 것을 확인한 뒤 RLS와 정책을 활성화한다.
5. 이전 번들 차단 시점과 롤백 절차를 정한 후에만 구형 호환 계층을 제거한다.

Q25/Q45에서 UI-only(B)를 선택하면 이 순서로 RLS를 적용할 수 없으므로, DB 권한 강제는 별도 인증/서버 검증 작업이 완료될 때까지 보류한다. 현재는 Q45=A·Q50=B를 선택했으므로, 서버 게이트웨이와 단기 세션 검증이 준비되기 전까지 RLS/RPC를 활성화하지 않는다.

## 5. 구현 단계

### Phase 0 — 결정과 기준선 고정

1. Q1~Q50 답변으로 확정된 역할, 권한 우선순위, 설정 목록, 상속, 데이터 변경 범위, 임직원 권한표, 서버 경계를 기준선으로 삼는다.
2. 현재 운영 DB의 계정·회사·메뉴·튜토리얼·상품 담당자 연결을 민감정보 없이 스냅샷한다.
3. `adminCapabilities.ts`의 계정 ID fallback, 폼별 기본값, `plannedDepositorName` 호출 지점, 브라우저 저장 key를 전수 목록화한다.
4. 기존 권한을 새 권한 모델로 변환하는 매핑표와 롤백 기준을 작성한다.
5. 새 테이블을 선택했으므로 DB 대상 허용 목록과 문서 갱신 범위를 먼저 확정하고, Q49~Q50에서 정한 세부 계약을 migration·RPC·게이트웨이 설계에 반영한다.

### Phase 1 — DB 계약과 마이그레이션

1. 선택된 모델에 맞춰 역할·회사·활성 상태를 저장한다. 임직원은 회사 소속만으로 연결하며 `parent_admin_id` 계층은 만들지 않는다.
2. 회사/역할/개인 권한과 메뉴·액션 권한을 저장하는 계약을 확정한다.
3. 회사 기본값과 개인 기본값을 타입이 보장되는 형태로 저장한다.
4. 예정 입금자명 자르기 길이와 제품비·리뷰비 기본값의 초기값을 `0`, 현재 업무 기본값으로 backfill한다.
5. 현재 계정 매핑과 현재 메뉴 권한을 보존하는 seed/backfill을 준비한다.
6. 정규화된 회사·권한·설정·감사 이력 구조와 기존 테이블 간 호환 계층을 준비한다.
7. 서버 측 권한 검사는 Q25 재확인(Q45)=A, 인증 경계는 Q50=B로 확정했다. Edge Function/서버 게이트웨이가 검증한 단기 세션만 RPC 권한 검사의 신원으로 사용하고, 현재 localStorage ID를 신뢰하는 정책은 만들지 않는다.
8. 선택된 대상만 대상으로 additive migration을 적용하고, 적용 후 실제 스키마·제약·RPC를 재조회한다.
9. 최종 상태에 맞춰 `docs/guide_db.md`와 샘플/row count를 갱신한다.

### Phase 2 — 권한 서비스와 라우팅 경계 (Q50=B 서버 게이트웨이)

1. 계정 ID 하드코딩 fallback을 제거하고 DB의 역할·권한 조회 서비스로 대체한다.
2. `AdminAccessContext`가 계정 프로필, 역할, 회사, 유효 권한, 관리 대상 범위를 공유하도록 확장한다.
3. `AdminLayout`에서 메뉴 표시와 직접 URL 접근을 같은 권한 resolver로 판정한다.
4. 메뉴가 보이지 않는 것과 액션을 실행할 수 있는 것을 분리하되 같은 DB 권한 코드에서 파생한다.
5. 메뉴 권한 로딩 중에는 보호된 본문을 렌더링하지 않고, 실패 시 오류와 재시도 또는 안전한 이동 경로를 제공한다.
6. 매핑되지 않은 신규 관리자 경로가 자동 허용되지 않도록 route-to-permission 매핑을 명시적으로 검증한다.
7. 회사 관리자에게는 같은 회사 employee만 관리 대상으로 반환하고, developer에게는 전체 계정·회사 관리 범위를 반환한다.
8. company_admin은 계정 생성·비활성화·회사 이동·역할 변경을 하지 않고, 외부 운영 절차로 생성된 계정의 권한과 기본값만 관리한다.

### Phase 3 — 개발자·회사 관리자 설정 화면

1. 개발자용 계정/회사 목록과 역할 계층 화면을 제공한다.
2. 회사 관리자는 같은 회사 employee 목록만 보고 권한과 허용된 기본값을 변경할 수 있게 한다.
3. 메뉴 권한과 액션 권한을 구분해 체크박스·상속 상태·개인 예외를 표시한다.
4. 변경 전후 차이, 상속 출처, 적용 대상 수를 확인 대화상자에서 보여준다.
5. 계정 생성·역할 변경은 외부 운영 절차임을 화면에 명확히 표시하고, 저장 중 중복 제출 방지, 권한 부족, 대상 변경 충돌, 네트워크 오류를 구분한다.
6. 기본 브라우저 `alert/confirm/prompt`는 사용하지 않고 공통 `AlertDialog` 계약을 사용한다.
7. 좁은 화면에서도 계정 목록·권한 표·상속 상태가 잘리지 않도록 가로 스크롤과 고정 열을 설계한다.

### Phase 4 — 임직원 개인 설정과 상품 기본값

1. 임직원 설정 화면에서 본인 웹 화면 설정을 조회·저장한다.
2. 개인 설정은 계정 ID로 분리하며 다른 계정의 브라우저 저장값과 섞이지 않게 한다.
3. 회사 기본값과 역할 기본값을 읽어 상품/품목 생성 폼의 초기값을 계산한다.
4. 현재 입력값이 기본값을 덮어쓰되 저장하지 않은 변경은 다른 작업에 영향을 주지 않게 한다.
5. 기존 설정 화면의 프로필·비밀번호 변경과 새 권한 관리 화면의 책임을 분리한다.

### Phase 5 — 예정 입금자명과 모든 입력 경로 통합

1. 회사 설정에서 자르기 글자 수를 읽고 순수 계산 유틸에 전달한다.
2. 리뷰받기 단건·품목·리뷰어 일괄 입력, 파일 업로드, 상품 수정 등 모든 생성/수정 경로를 점검한다.
3. `0`, 빈 값, 음수, 숫자가 아닌 값, 회사명이 없는 값, 회사명보다 큰 값의 처리 규칙을 동일하게 적용한다.
4. 신규 저장값과 기존 저장값의 재계산 정책을 구분한다.
5. Excel 내보내기·상품전체보기·리뷰받기 상세의 표시값이 서로 달라지지 않는지 확인한다.

### Phase 6 — 하드코딩 제거와 호환성 정리

1. `hyejin2054`, `aram2525`, `kimhanbi77`를 직접 비교하는 capability fallback을 제거한다.
2. 계정별 메뉴·권한·기본값 seed는 런타임 코드가 아니라 DB migration/backfill 데이터로 이동한다.
3. 사용하지 않는 `review_manager_include_company_data` 브라우저 key와 계정 공용 export key의 처리 정책을 정리한다.
4. 메뉴 번호·route path·권한 코드 같은 기술적 식별자는 Q19 결정에 따라 유지하거나 DB 메뉴 카탈로그로 분리한다.
5. Q25/Q45=A·Q50=B로 확정된 현재 계획에서는 이전 클라이언트가 남아 있어도 새 DB 권한을 우회하지 못하도록 서버 게이트웨이 검증과 RPC를 먼저 적용한다. 게이트웨이 전환 전에는 기존 클라이언트에 restrictive RLS를 적용하지 않는다.

### Phase 7 — 보안·운영 적용

1. 현재 `admins` 직접 조회 로그인은 Q50=B 서버 게이트웨이와의 호환 기간에만 유지하고, 최종 Supabase Auth 전환은 Q17 결정대로 후속 범위에 둔다.
2. Q25/Q45=A인 현재 계획에서는 서버 검증을 먼저 배포해 브라우저에서 ID만 바꿔도 다른 계정으로 가장할 수 없도록 한다.
3. Q50에서 정한 인증 주체와 새 클라이언트·호환 계층을 검증한 뒤 RLS 또는 권한 검증 RPC를 활성화하고, 조회·생성·수정·삭제·민감 액션마다 같은 회사/역할 규칙을 적용하는지 확인한다.
4. 권한 변경·기본값 변경의 감사 이력이 필요한 경우 Q18 승인 범위 안에서 설계한다.
5. 운영 적용은 schema → 권한 서비스 → UI 순서로 하고, 실패 시 이전 UI가 안전하게 읽기 전용으로 동작하는지 확인한다.

### Phase 8 — 검증과 문서

1. 역할별 메뉴 표시, 직접 URL, 로딩·오류·권한 없음 상태를 검증한다.
2. 개발자가 모든 회사 계정을 관리하는지, 회사 관리자가 같은 회사 임직원만 관리하는지 검증한다.
3. 개인 거부·회사 허용·역할 기본값·권한 없음 조합을 검증한다.
4. 입금확인, 일괄수정, 파일 업로드, 상품 생성·수정·삭제의 UI와 DB 검사를 함께 검증한다.
5. 제품비/리뷰비 기본값, 회사명 자르기, `0` 글자, 기존 데이터 정책을 검증한다.
6. 계정 전환·로그아웃·새로고침·다른 브라우저에서 설정이 섞이지 않는지 확인한다.
7. 관련 Node 테스트, `npm run build`, `npm run supabase:check`, 주요 경로의 브라우저 검증을 실행한다.
8. 실제 적용 결과와 row count를 `docs/guide_db.md`에 기록한다.

### 코드 구현 세부 단계 (실제 작업 순서)

아래 단계는 위의 운영 호환성 원칙을 지키면서 실제 소스 코드를 변경하는 순서다. 각 단계의 완료 조건을 통과하기 전에는 다음 단계나 운영 DB의 다음 전환으로 넘어가지 않는다.

#### C0. 기준선 고정과 회귀 기록

1. `docs/project_analysis.md`, `docs/guide_db.md`, `SUPABASE_SETUP.md`의 현재 계약과 허용 테이블을 다시 확인한다.
2. `npm test`, `npm run build`, `npm run supabase:check`를 실행하고 결과를 기준선으로 저장한다.
3. 운영 DB의 계정·회사·메뉴 권한·capability·튜토리얼·상품 담당자 연결과 주요 row count를 민감정보 없이 스냅샷한다.
4. 현재 운영 웹 번들로 로그인, 주요 관리자 메뉴, 상품/제출 조회·저장, 입금확인, 파일 업로드, 내보내기, 일괄수정, 공개 리뷰받기 사진 흐름을 smoke test한다.
5. 현재 브라우저 저장 key와 계정별 유효 권한을 기록해 새 코드의 기대 결과와 비교할 회귀 목록을 만든다.

완료 조건: baseline 결과와 rollback 대상 웹 이미지/마이그레이션 버전이 기록되고, 기존 기능의 기준 동작이 재현된다.

#### C1. 도메인 계약과 순수 권한 계산

1. `src/constants/admin.ts`의 route·메뉴 번호 매핑은 유지하고, 메뉴 코드·resource-action 코드·역할·효과(`allow/deny`)·데이터 범위 enum을 단일 계약으로 정리한다.
2. 기존 책임을 재사용할 수 있으면 `src/services/*`, `src/utils/*`, `src/contexts/*`에 추가하고, 새 파일이 필요할 때만 권한 계약·설정 계약·오류 코드처럼 독립적으로 테스트할 수 있는 최소 모듈을 만든다.
3. 순수 `permissionResolver`는 회사·역할·개인 규칙의 상속, 더 구체적인 규칙 우선, 같은 수준의 명시적 거부 우선, 미지 규칙 기본 거부를 계산한다.
4. 순수 `settingsResolver`는 회사 기본값 → 역할 기본값 → 개인 override → 현재 입력값 순서와 override 해제 시 회사 기본값 복귀를 계산한다.
5. `adminScope`의 본인/회사/전체 범위를 permission resolver의 데이터 범위와 연결하되, 계산 가능한 값을 React state에 중복 저장하지 않는다.

완료 조건: 네트워크 없이 역할·범위·충돌·기본값 상속 테스트가 통과하고, 기존 메뉴 번호·경로 문자열의 동작이 바뀌지 않는다.

#### C2. 운영 DB additive schema와 호환 RPC

1. `companies`, `permission_definitions`, `permission_bindings`, `settings_definitions`, `settings_values`, `admin_change_audits`와 필요한 기존 테이블 컬럼을 명시적 migration으로 추가한다. 새 테이블은 Q1·Q27에서 정한 허용 범위와 `docs/guide_db.md`에 함께 반영한다.
2. 기존 `admins`, `admin_menu_permissions`, `products`, `applications`, `submissions`, `evidence_photos`, `product_steps`의 기존 컬럼·인덱스·RPC 시그니처는 보존한다. 기존 웹 번들이 읽는 필드는 제거·이름변경하지 않는다.
3. 역할·회사·메뉴·capability·기본값을 새 구조로 idempotent backfill하고, Q49의 현행 동작과 일치하는 계정별 seed를 만든다.
4. 기존 RPC를 직접 변경하기보다 새 RPC 또는 wrapper를 추가한다. 새 RPC의 입력·출력·권한·허용 테이블 의존성을 문서화하고, 페이지네이션·1,000건 초과·큰 `IN` 분할 계약을 유지한다.
5. backfill 전후 row count·중복·foreign key·unique·check constraint·인덱스·RPC 결과를 비교한다. 검증되지 않은 constraint는 운영 트래픽에 영향을 주지 않는 방식으로 단계적으로 validate한다.
6. migration 적용 직후에도 기존 웹 번들로 C0 smoke test를 반복한다. 실패하면 새 코드 작업을 중지하고 호환 migration 또는 rollback을 먼저 처리한다.

완료 조건: 새 구조가 없어도 기존 번들이 계속 동작하고, 새 구조를 추가해도 기존 API 응답·메뉴·데이터가 달라지지 않는다.

#### C3. Q50=B 서버 게이트웨이와 세션 경계

1. 기존 로그인 UI는 호환 기간 동안 유지하되, 서버/Edge Function 게이트웨이에서 자격 증명을 검증하고 짧은 만료의 서명 세션 또는 httpOnly 쿠키를 발급한다. 공개 리뷰받기 페이지의 anon 조회·사진 동기화는 기존 공개 경계를 그대로 유지한다.
2. 브라우저가 `localStorage`의 `adminId`를 관리자 권한 신원으로 직접 제출하는 경로를 새 코드에서 제거한다. 클라이언트가 보낸 `p_admin_id`만으로 권한을 결정하지 않는다.
3. 게이트웨이는 검증된 세션의 principal을 기준으로 권한 resolver와 scope를 실행하고, 허용된 RPC만 호출한다. service role secret은 브라우저에 노출하지 않는다.
4. 연결 실패, 만료 세션, 입력 오류, 권한 부족, 서버 오류를 서로 다른 오류 코드로 내려 기존 UI가 빈 화면 대신 재로그인·재시도·권한 없음 상태를 표시하게 한다.
5. 게이트웨이 운영 설정과 배포·로그·health check·rollback 절차를 `SUPABASE_SETUP.md`와 `docs/guide.md`에 기록한다.

완료 조건: 개발자/테스트 계정으로 세션 위조·계정 ID 변경·직접 테이블 호출이 권한 우회로 이어지지 않고, 기존 로그인 실패·로그아웃·세션 만료 흐름이 사용자에게 설명 가능한 상태로 표시된다.

#### C4. 데이터 접근 서비스의 게이트웨이 전환

현재 로컬 checkpoint: 아래 관리자 데이터 서비스의 gateway-first adapter, 기존 반환 계약 정규화, operation allowlist, nested client identity 제거를 구현했다. `AdminProductOverview`, `AdminBulkEdit`, `AdminFileUpload`, export 화면과 리뷰받기·상품 상세의 생성/수정/삭제/입금/사진 action에 같은 resolver 기반 검사를 연결했고, 목록 호출 전 상품·제출 조회 권한도 확인한다. Edge Function 초안은 session principal과 access bundle로 operation 최소 권한을 재검사한다. `supabase/migrations/20260829120000_add_admin_gateway_data_rpcs.sql`에는 legacy RPC 시그니처를 바꾸지 않는 gateway 전용 data RPC와 공통 resource/field scope 검사, payload allowlist, service-role 전용 execute 권한 초안을 추가했고, `supabase/migrations/20260829150000_backfill_admin_resource_permission_bindings.sql`에는 Q49 메뉴/capability 기반 admin binding backfill 초안을 추가했다. staging에는 `20260829180000_optimize_admin_gateway_read_rpcs.sql`과 `20260829200000_optimize_admin_gateway_export_read_rpcs.sql`까지 적용해 고사용량 read RPC를 보강했다. 상품/리뷰어 일괄입력의 `bundle_id` 계약 후속 migration과 adapter 오류 메시지 보완도 추가했으며 production에는 별도 승인 후 적용했다. `hyejin2054` 인증 canary에서 대시보드·리뷰받기·상품전체보기·일괄수정·export 화면, 상품·제출 생성/수정/상태 변경/삭제·일괄수정과 gateway-only 네트워크를 확인했고, canary row 정리도 DB에서 재확인했다. 공개 사진 흐름·RLS·production 전체 전환은 남아 있으므로 C4 완료 조건은 아직 충족하지 않는다. 상세 operation/RPC 표는 `docs/operations/admin-gateway-data-contract.md`에 기록한다.

1. `src/services/adminAuth.ts`는 로그인·프로필·메뉴 조회의 새 transport를 사용하도록 바꾸되, 호환 기간에는 기존 응답 필드를 adapter로 유지한다.
2. `adminProducts.ts`, `productOverview.ts`, `productDetail.ts`, `reviewReceive.ts`, `bulkEdit.ts`, `fileUpload.ts`, `exportData.ts`, `exportPhotos.ts`, `dashboardMetrics.ts`, `adminDeletion.ts`, `evidencePhotos.ts`의 Supabase 직접 조회·쓰기를 서비스 단위로 게이트웨이/RPC 호출로 교체한다.
3. 컴포넌트에 쿼리를 새로 흩뿌리지 않고 기존 서비스 함수 이름과 반환 계약을 가능한 한 유지해 페이지·훅의 변경 범위를 줄인다.
4. 조회는 필요한 컬럼만 선택하고 `paginatedQuery.ts`의 커서 반복·chunk 분할을 재사용한다. 목록 RPC는 같은 필터·정렬·cursor로 다음 페이지를 이어 간다.
5. 생성·수정·삭제·입금확인·사진 삭제·일괄수정은 서버에서 scope와 resource-action 권한을 다시 확인하고, 부분 성공·멱등성·중복 제출·롤백 가능한 오류 결과를 기존 화면 계약에 맞춰 반환한다.

완료 조건: 각 서비스가 직접 client identity를 신뢰하지 않고, 성공·권한 거부·입력 오류·네트워크 오류 테스트가 통과한다.

#### C5. 권한 컨텍스트·라우팅·메뉴 적용

1. `src/contexts/AdminAccessContext.tsx`, `src/hooks/useAdminCapabilities.ts`, `src/components/layout/AdminLayout.tsx`, `src/App.tsx`를 확장해 세션 principal, 프로필, 역할, 회사, 유효 권한, 관리 대상 범위를 한 번만 로드한다.
2. `admin_menu_permissions` 호환 행과 새 권한 binding을 resolver로 합쳐 메뉴 표시와 직접 URL 접근이 같은 결과를 내도록 한다.
3. 메뉴 번호 1~7 및 하위 경로 매핑을 `src/constants/admin.ts` 단일 기준으로 유지하고, 매핑되지 않은 `/admin/*` 경로는 기본 거부한다.
4. 권한 로딩 중 보호된 본문을 렌더링하지 않고, 실패·만료·권한 없음은 공통 오류/이동 상태로 처리한다.
5. 페이지 action 버튼은 메뉴 노출과 별도로 resource-action 권한을 확인하되 같은 resolver와 서버 RPC 결과를 사용한다.
6. developer는 전체 회사, company_admin은 같은 회사 employee, employee는 Q49 현행 범위만 반환하도록 scope를 적용한다.

완료 조건: 메뉴를 숨긴 계정의 직접 URL·버튼·서버 쓰기가 모두 차단되고, 허용 계정의 기존 메뉴·하위 경로 동작은 유지된다.

#### C6. 관리자·임직원 설정 화면과 상태 저장

1. `src/pages/admin/AdminSettingPage.tsx`와 관련 공통 UI를 회사 기본값/개인 override/프로필 영역으로 분리한다. 권한 관리 화면은 developer·company_admin 범위만 렌더링한다.
2. `src/services/adminSettings.ts`와 새 설정 조회·저장 hook은 DB를 원본으로 사용하고, Q44 타입·단위·범위 검증과 Q48의 설정·해제·긴급 초기화 audit을 적용한다.
3. 기존 `browserStorage` 사용처를 계정별 key로 이관하고, 1회 migration 후 DB 값을 우선한다. 네트워크 오류 때만 메모리 fallback을 사용하며 다른 계정의 값을 재사용하지 않는다.
4. 회사 기본값 변경은 company_admin/developer만, 개인 override 변경은 employee 본인만 허용한다. 외부 계정 생성·역할·회사 이동은 화면에서 수행하지 않는다.
5. 저장 중 busy/중복 제출, 권한 부족, validation, 네트워크 오류를 구분하고 공통 Dialog 계약을 사용한다.

완료 조건: 새로고침·로그인·계정 전환·다른 브라우저에서 설정이 섞이지 않고, 기본값 변경이 기존 상품·제출을 소급 변경하지 않는다.

#### C7. 상품 기본값과 예정 입금자명 통합

1. `src/utils/plannedDepositorName.ts`를 유니코드 문자·`0=전체`·1~100·초과 입력 오류 계약에 맞춰 순수 함수로 고정한다.
2. `AdminReviewReceivePage`, `AdminReviewReceiveDetailPage`, `AdminProductDetailPage`, `AdminFileUploadPage`, 상품 일괄 입력·수정 경로에서 동일한 설정 resolver와 계산 유틸을 사용한다.
3. 제품비/리뷰비 입금구분·리뷰형태·실배송 여부·리뷰비 기본 금액은 새 상품/품목·새 submission 입력에만 복사하고, 기존 저장값은 자동 덮어쓰지 않는다.
4. 상품 `company_name` 우선, 없으면 관리자 회사명 fallback을 적용하고, 날짜·회사명·입금구분·자르기 관련 필드 변경 때만 `planned_depositor_name`을 재계산한다.
5. 파일 업로드·일괄수정·내보내기·상품전체보기·리뷰받기 상세의 표시와 저장값이 일치하는지 확인한다.

완료 조건: 신규 생성·재저장·대량 입력·수정의 모든 경로가 같은 결과를 내고, 기존 상품/제출의 저장된 예정 입금자명이 임의로 바뀌지 않는다.

#### C8. 하드코딩 제거와 호환 계층 전환

1. 새 DB/게이트웨이 데이터가 모든 계정에서 검증된 뒤 `adminCapabilities.ts`의 계정 ID fallback과 페이지별 capability 예외를 제거한다.
2. 런타임 계정 ID 비교, 브라우저 공용 export key, legacy company-scope key를 검색해 제거·계정별 migration·읽기 전용 호환 중 하나로 정리한다.
3. 기존 `admin_menu_permissions`와 capability 컬럼은 전환 기간 read-only 호환 계층으로 두고, 새 권한 binding이 기준임을 로그·문서에 남긴다.
4. 새 웹 번들에서 모든 관리자 데이터 접근이 게이트웨이를 통과하는지 네트워크와 코드 검색으로 확인한다.
5. 구형 번들 업데이트 안내, 최대 캐시/세션 수명, rollback window를 운영에 적용한 후에만 기존 호환 계층과 restrictive RLS를 단계적으로 정리한다.

완료 조건: 계정 ID를 코드에서 바꾸거나 브라우저 저장값을 조작해 권한이 바뀌지 않고, 기존 운영 데이터·메뉴·튜토리얼이 보존된다.

#### C9. 통합 검증·canary·운영 전환

1. 순수 유틸·권한 resolver·설정 resolver·페이지네이션·Excel parser 테스트를 먼저 통과시킨다.
2. `npm run build`, `npm run supabase:check`, 기존 Node 테스트를 실행하고, 실패 시 운영 DB 전환을 중단한다.
3. staging에서 migration → gateway → 새 bundle 순서로 검증하고, developer·각 company_admin·시나브로 employee·테스트 계정으로 권한 matrix를 확인한다.
4. canary 운영에서는 로그인, 대시보드, 리뷰받기, 상품전체보기, 상품 생성/수정/삭제, 입금확인, 파일 업로드, 내보내기, 일괄수정, 공개 리뷰받기 사진 흐름을 기존 baseline과 비교한다.
5. 구형 번들 접속·오픈 탭·캐시가 남아 있는 동안에는 restrictive RLS를 활성화하지 않는다. 모든 활성 클라이언트가 게이트웨이로 이동하고 rollback window가 끝난 뒤 정책을 강화한다.
6. RLS/RPC 활성화 후 관리자 직접 테이블 접근, 다른 회사 데이터 조회, employee의 권한 변경, 입금확인 우회, ID 변조를 부정 테스트하고, 공개 `/review-receive/*` anon 조회·사진 업로드/삭제 재시나리오를 별도로 통과시킨다.
7. 운영 DB 적용 직전 backup/row count/schema snapshot을 남기고, 적용 직후 같은 체크와 브라우저 smoke test를 실행한다.
8. 이상 발생 시 단계별 rollback(웹 이미지, 게이트웨이, 호환 RPC/정책)을 실행하고, 해결 전에는 destructive cleanup을 하지 않는다.
9. 최종 migration·RPC·Edge Function·환경변수·실제 row count를 `docs/guide_db.md`, `SUPABASE_SETUP.md`, `docs/guide.md`에 동기화한다.

완료 조건: 기존 baseline 대비 사용자 가시 동작의 회귀가 없고, 권한 우회·데이터 누락·부분 쓰기 문제가 없으며, rollback 절차가 실제로 실행 가능하다.

## 6. 하드코딩 제거 범위

### 반드시 DB로 이동할 것

- 특정 계정 ID에 따른 회사범위 기본값
- 특정 계정 ID에 따른 입금확인 가능 여부
- 특정 계정·회사·역할의 메뉴 권한
- 회사 관리자와 임직원 역할·소속·상위 관리자
- 회사명 자르기 글자 수
- 제품비·리뷰비 입금구분 등 사용자 변경 가능한 생성 기본값
- 향후 추가될 계정별 웹 화면 설정

### 기술적으로 코드에 남길지 결정할 것

- route path와 메뉴 번호의 매핑
- 메뉴의 안정적인 permission code
- 제품 입금구분의 허용 enum과 표시 순서
- `MMDD` 날짜 형식 자체
- `업체페이` 같은 업무 문구/특수 동작

사용자가 말한 “하드코딩 제거”를 계정별 운영값까지 DB로 옮긴다는 의미로 볼지, route·enum 같은 변경 불가 기술 계약까지 DB로 옮긴다는 의미로 볼지는 Q19에서 확정한다.

## 7. Q1~Q50 답변 반영 결과

현재까지의 답변으로 다음 내용을 확정한다.

- DB는 정규화된 별도 회사·권한·설정 구조를 사용하는 안 B를 선택한다. 단, 운영 DB에 additive migration과 backfill을 먼저 적용해 기존 클라이언트 호환성을 보장한다.
- 초기 역할은 `2sssg=developer`, `hyejin2054=company_admin`, 시나브로의 나머지 계정은 `employee`, `test1/2/3=테스트커머스 company_admin`이다.
- 한 회사에 여러 `company_admin`을 둘 수 있고, 임직원은 회사 소속으로만 연결한다. 별도 상위 관리자 트리는 두지 않는다.
- 메뉴뿐 아니라 조회·생성·수정·삭제·확인·내보내기·일괄수정 등의 resource-action 권한을 관리한다.
- 더 구체적인 권한이 우선하고 같은 수준에서 명시적 거부가 우선한다. 권한 규칙이 없으면 기본 거부한다.
- company_admin은 같은 회사의 `employee`만 관리한다. 계정 생성·비활성화·회사 이동·역할 변경은 외부 운영 절차로 처리한다.
- 임직원은 본인의 웹 설정·상품 기본값·프로필·비밀번호를 수정할 수 있고 권한은 수정하지 못한다.
- 웹 설정은 정규화된 설정 항목/값 구조로 저장하며, 1차 항목은 대시보드 기간, 리뷰받기 탭/범위, 내보내기 컬럼, 사이드바 접힘이다.
- 상품 기본값은 제품비/리뷰비 입금구분, 리뷰형태, 실배송 여부, 리뷰비 기본 금액을 대상으로 하며 현재 `deposit_GB` 조합 계약과 호환한다.
- 회사·역할·개인 기본값은 최신 상속값을 새 상품/품목을 열 때만 적용하고 기존 상품은 자동 변경하지 않는다. 회사 기본값은 저장 즉시 적용한다.
- 회사명 자르기는 회사 기본값과 임직원 개인 override를 모두 지원한다. 유니코드 문자 기준, `0=전체`, 잘못된 값은 `0`, 상한 100으로 한다.
- 예정 입금자명은 상품 `company_name`을 우선하고 없을 때 관리자 회사명을 사용하며, 계산 결과를 `planned_depositor_name`에 저장한다. 기존 값은 보존한다.
- Q17=C에 따라 전면적인 Supabase Auth 전환은 후속으로 두되, Q25 재확인(Q45)=A와 Q50=B에 따라 이번 범위에서 서버 게이트웨이·단기 세션·RPC 권한 검사를 포함한다. 브라우저가 localStorage ID를 신원으로 직접 제출하는 방식은 제거한다.
- 권한·회사 기본값 변경 이력은 남긴다. route path, 안정적인 메뉴 코드, enum은 코드에 유지한다.

Q26~Q50 답변으로 다음 세부 계약도 확정했다.

- developer는 전 회사의 계정·권한·운영 데이터를 관리하고, company_admin은 같은 회사 employee만 관리한다. 회사 관리자끼리나 역할·회사 이동은 변경하지 않는다.
- 기존 메뉴 권한은 resource-action 권한으로 backfill하며, DB를 웹 설정의 원본으로 삼고 브라우저 저장값은 1회 이관 후 제거한다.
- 회사 기본값은 새 상품/제출에만 적용하고 기존 데이터는 보존한다. 개인 override는 유지하며 권한·역할·회사/기본값 변경은 감사 이력으로 남긴다.
- 기존 구조는 additive migration과 호환 계층으로 유지하고 staging 검증 후 운영에 전환한다. 마지막 developer 보호와 설정 타입·단위·범위 검증을 둔다.
- 임직원 최초 resource-action 권한은 “지금과 동일”하게 유지한다. 기존 메뉴 행·계정별 capability·담당자 범위를 스냅샷해 새 권한 행으로 backfill하고, 기존에 없던 권한은 추가하지 않는다.
- 관리자 DB/RPC 권한 강제는 서버 게이트웨이를 통해 수행한다. 게이트웨이 밖의 관리자 브라우저 직접 테이블 접근과 클라이언트 제공 `p_admin_id` 신뢰는 허용하지 않으며, 공개 리뷰받기 흐름은 기존 anon/사진 동기화 계약으로 별도 보호한다.

현재 추가로 답변이 필요한 질문은 없다. 구현 단계에서 Q49 표의 “현행” 값을 코드·운영 데이터로 스냅샷하고, Q50=B의 게이트웨이 세션 계약을 실제 배포 환경에 맞춰 설계한다. production bundle 계약 후속 RPC와 app-only 웹 수정은 2026-08-29 별도 승인으로 반영했으며, 실제 운영 저장 canary는 브라우저 세션 재로그인 후 남아 있다.

## 8. 기존 질문·답변 기록

아래 Q1~Q50은 사용자가 답변한 기록이다. 선택 결과는 위의 답변 반영 결과와 각 질문 아래에 함께 기록되어 있다.

### Q1. DB 테이블 범위

권한·회사·설정 저장 구조를 어느 수준으로 정규화할까?

- **A (현재 범위 우선, 추천)**: 현재 허용된 8개 테이블만 확장한다. `admins`와 `admin_menu_permissions`에 필요한 컬럼/권한 계약을 추가한다.
- **B (장기 구조 우선)**: 회사·권한·설정용 별도 테이블을 허용한다. 이 경우 AGENTS.md의 DB 허용 목록 확장과 별도 승인부터 진행한다.

Answer: B, 다만 DB 테이블 및 컬럼 변경 시 운영DB에 바로 적용되므로 호환성에 문제가 생기면 안되는 점을 인지

### Q2. 실제 역할 배정

현재 계정을 어떤 역할로 시작할까?

- **A (안전한 기본안, 추천)**: `2sssg=developer`, `hyejin2054=company_admin`, `aram2525/kimhanbi77/tjsal9601=employee`, `test1/2/3=테스트커머스 company_admin`.
- **B**: `2sssg/test1/test2/test3=developer`, `hyejin2054=company_admin`, 시나브로 나머지는 employee.
- **C**: 개발자 역할은 두지 않고 `2sssg`와 테스트 계정을 각 회사의 company_admin으로 둔다.

Answer: A

### Q3. 회사 관리자 수와 상위 계층

한 회사에 회사 관리자를 몇 명 둘 수 있을까?

- **A (단일 관리자, 추천)**: 회사마다 1명의 company_admin을 두고 모든 임직원을 관리한다.
- **B**: 여러 company_admin을 허용하고 동일 권한으로 관리한다.
- **C**: 여러 관리자를 허용하되 상위 관리자/하위 관리자 트리를 둔다.

Answer: B

### Q4. 임직원 소속 방식

임직원이 회사를 어떻게 연결할까?

- **A (단순 모델, 추천)**: `company` 소속만 저장하고 회사 관리자는 같은 회사 전체를 관리한다.
- **B**: 회사 소속과 별도로 `parent_admin_id`를 저장해 담당 관리자를 명시한다.
- **C**: 한 임직원이 여러 회사/조직에 소속될 수 있게 한다.

Answer: A

### Q5. 권한 세분화

메뉴 외에 어떤 액션까지 권한으로 관리할까?

- **A**: 메뉴 표시/접근과 `입금확인` 등 현재 필요한 소수의 민감 액션만 관리한다.
- **B (추천)**: 메뉴 + 조회/생성/수정/삭제/확인/내보내기/일괄수정 등 resource-action 권한으로 설계한다.
- **C**: 모든 버튼을 별도 권한으로 만들고 세밀하게 관리한다.

Answer: B

### Q6. 권한 상충 우선순위

회사에서 허용했지만 개인에게 거부하는 등 규칙이 충돌하면 무엇을 우선할까?

- **A (보안 우선, 추천)**: 더 구체적인 규칙이 우선하며, 같은 수준에서는 명시적 거부가 허용보다 우선한다.
- **B**: 허용이 하나라도 있으면 허용한다.
- **C**: 회사 → 역할 → 개인 순서로 마지막 규칙이 우선한다.

Answer: A

### Q7. 규칙이 없을 때 기본값

권한 행이 없는 새 메뉴/액션은 어떻게 할까?

- **A (안전 우선, 추천)**: 기본 거부. 필요한 권한을 명시적으로 부여한다.
- **B (기존 호환 우선)**: 기존 메뉴는 허용하고 신규 메뉴만 기본 거부한다.
- **C**: 역할별 기본 허용 목록을 별도로 정한다.

Answer: A

### Q8. 회사 관리자의 관리 대상

시나브로의 `hyejin2054`가 어디까지 변경할 수 있어야 할까?

- **A (요청 그대로, 추천)**: 같은 회사의 employee만 관리한다.
- **B**: 같은 회사의 employee와 다른 company_admin도 관리한다.
- **C**: 본인 프로필/기본값만 관리하고 권한 변경은 developer만 한다.

Answer: A

### Q9. 임직원 자기 설정

임직원이 본인 설정을 어디까지 수정할 수 있을까?

- **A (추천)**: 웹 화면 설정·상품 기본값·프로필/비밀번호는 본인이 수정하고 권한은 수정하지 못한다.
- **B**: 웹 화면 설정과 상품 기본값만 수정하고 프로필/비밀번호는 관리자만 수정한다.
- **C**: 모든 설정을 회사 관리자의 승인 후 반영한다.

Answer: A

### Q10. 설정 저장 형태

추후 항목이 늘어날 웹 설정을 어떻게 저장할까?

- **A (타입 안정성 우선, 추천)**: 자주 쓰는 핵심값은 명시적인 컬럼으로 저장하고, 제한된 보조 설정만 버전 있는 JSON으로 저장한다.
- **B**: 모든 설정을 허용 키 목록이 있는 JSON 한 덩어리로 저장한다.
- **C**: Q1에서 B를 선택한 경우 정규화된 설정 항목/값 구조를 사용한다.

Answer: C

### Q11. 임직원 웹 설정의 1차 항목

첫 구현에 어떤 화면 설정을 포함할까? 여러 개를 골라도 된다.

- **A (추천)**: 대시보드 초기 기간, 리뷰받기 기본 탭/범위, 내보내기 컬럼, 사이드바 접힘
- **B**: A + 표 페이지 크기/정렬/필터 저장
- **C**: A/B 모두 제외하고 상품 생성 기본값만 먼저 구현
- **D**: 위 항목 외에 직접 목록 지정

Answer: A

### Q12. 제품 생성 기본값

DB에 저장할 1차 상품/품목 기본값은 무엇일까?

- **A (추천)**: 제품비 입금구분, 리뷰비 입금구분, 리뷰형태, 실배송 여부, 리뷰비 기본 금액
- **B**: A + 기본 회사명/옵션/설명 템플릿
- **C**: 제품비·리뷰비 입금구분만 먼저 구현
- **D**: 직접 필드 목록 지정

Answer: A

### Q13. 기본값 상속과 적용 시점

회사/역할/개인 기본값이 바뀌면 어떻게 할까?

- **A (추천)**: 새 상품/품목을 열 때 최신 상속값을 사용하고, 이미 저장된 상품은 바꾸지 않는다.
- **B**: 회사 기본값 변경 시 기존 상품의 아직 진행 전인 값도 일괄 갱신한다.
- **C**: 생성 시점에 모든 기본값을 복사하고 이후 상속을 사용하지 않는다.

Answer: A

### Q14. 회사명 자르기 적용 범위

회사명 자르기 글자 수는 누가 설정하고 어디에 적용할까?

- **A (추천)**: 회사 단위 설정. company_admin과 developer가 변경하며 해당 회사 신규 생성값에 적용한다.
- **B**: 임직원 개인 설정. 계정마다 다르게 표시한다.
- **C**: 회사 기본값을 두되 임직원이 개인적으로 덮어쓸 수 있게 한다.

Answer: C

### Q15. 회사명 자르기 입력 규칙

글자 수의 단위와 잘못된 값 처리는 어떻게 할까?

- **A (추천)**: 유니코드 문자 수 기준, `0=전체`, 음수/빈 값/비정상 값은 `0`, 큰 값은 전체. 상한은 100.
- **B**: DB/JavaScript 모두 grapheme 단위로 처리해 이모지 결합문자까지 한 글자로 센다.
- **C**: 숫자만 허용하고 잘못된 입력은 저장을 막는다.

Answer: A

### Q16. 기존 예정 입금자명 처리

설정을 도입할 때 이미 저장된 `planned_depositor_name`을 어떻게 할까?

- **A (데이터 보존, 추천)**: 기존 값은 유지하고 신규 생성/사용자가 다시 저장하는 값부터 적용한다.
- **B**: 마이그레이션 시 회사별 규칙으로 기존 값을 일괄 재계산한다.
- **C**: 미입금/미진행 상품만 재계산하고 완료 상품은 유지한다.

Answer: A

### Q17. 인증·서버 권한 전환 범위

현재의 `admins` 직접 비교와 localStorage ID 구조를 언제 바꿀까?

- **A (보안 우선, 추천)**: Supabase Auth 또는 서버 세션 기반 인증과 RLS/RPC 검사를 이번 개편에 포함한다.
- **B**: 이번에는 DB 권한/RPC 검사를 먼저 넣고 기존 로그인은 임시 유지한다.
- **C**: 인증은 이번 범위에서 제외하고 UI 권한만 DB화한다. (운영 보안상 권장하지 않음)

Answer: C

### Q18. 권한 변경 이력

누가 누구의 권한·기본값을 언제 바꿨는지 기록할까?

- **A (추천)**: 권한/회사 기본값 변경 이력을 남긴다. 별도 audit 저장 구조가 필요하면 Q1의 DB 범위도 함께 확장한다.
- **B**: 초기 버전에서는 이력 없이 현재값만 저장한다.
- **C**: 권한 변경만 기록하고 일반 기본값 변경은 기록하지 않는다.

Answer: A

### Q19. 메뉴·도메인 상수의 하드코딩 범위

“하드코딩 제거”를 어디까지 적용할까?

- **A (추천)**: 계정·회사·역할·권한·설정값만 DB화하고 route path, 안정적인 메뉴 코드, enum은 코드에 둔다.
- **B**: 메뉴 목록·라벨·경로까지 DB에서 관리한다.
- **C**: 메뉴 라벨/노출만 DB화하고 route path는 코드에 둔다.

Answer: A

### Q20. 회사 관리자의 기본값 변경 승인

회사가 여러 임직원의 기본값을 바꿀 때 즉시 적용할까?

- **A (추천)**: 저장 즉시 적용하되 새로 생성하는 상품부터 적용한다.
- **B**: developer 승인 후 적용한다.
- **C**: 임직원별 수락/거부 절차를 둔다.

Answer: A

### Q21. 계정 생성·비활성화·역할 변경 주체

새 임직원 계정 생성, 비활성화, 회사 이동, 역할 변경을 누가 할 수 있을까?

- **A (추천)**: developer는 전체 계정을 관리하고, company_admin은 같은 회사 employee의 생성·비활성화·기본 정보만 관리하며 역할 승격은 developer만 한다.
- **B**: company_admin도 같은 회사 안에서 company_admin/employee 역할을 모두 변경할 수 있다.
- **C**: 계정 생성·역할 변경은 외부 운영 절차로 하고 백오피스에서는 권한만 관리한다.

Answer: C

### Q22. 예정 입금자명의 회사명 출처

자르기 대상인 “회사명”은 어느 값을 사용할까? 현재 코드에는 관리자 회사(`admins.company`)와 상품 회사명(`products.company_name`)이 둘 다 있다.

- **A (기존 동작 보존, 추천)**: 상품/품목의 `company_name`을 사용하고, 값이 없을 때만 관리자 회사명을 fallback으로 사용한다.
- **B**: 로그인 계정의 `admins.company`만 사용한다.
- **C**: 상품 회사명과 관리자 회사를 다르게 허용하지 않고 저장 시 하나로 맞춘다.

Answer: A

### Q23. 예정 입금자명의 저장·표시 방식

자른 이름을 DB에 저장할지, 표시할 때만 자를지 결정해야 한다.

- **A (현재 구조 호환, 추천)**: 생성 시 계산한 결과를 `products.planned_depositor_name`에 저장하고, 설정 변경은 신규/재저장 값에만 적용한다.
- **B**: DB에는 원본 회사명만 보존하고 화면·Excel 출력 시에만 자른다.
- **C**: 원본과 계산 결과를 모두 저장해 원본 복구와 표시 최적화를 지원한다.

Answer: A

### Q24. 제품비·리뷰비 기본값의 DB 표현

현재 DB의 `products.deposit_GB`는 제품비/리뷰비 조합을 1~4 숫자로 저장한다. 기본값도 같은 계약을 사용할까?

- **A (기존 계약 보존, 추천)**: 기본값은 내부적으로 `deposit_GB` 조합을 사용하고, UI에서 제품비/리뷰비 두 선택값으로 보여준다.
- **B**: `default_product_deposit_gb`, `default_review_deposit_gb` 두 컬럼으로 분리하고 저장 시 기존 조합값으로 변환한다.
- **C**: 제품비·리뷰비 기본값을 완전히 별도 권한/설정 리소스로 정규화한다. 이때 `업체페이`도 설정 가능한 표시 규칙으로 둘지 함께 결정한다.

Answer: A

### 추가 질문 Q25~Q50 (답변 반영 완료)

아래 질문은 Q1~Q24 답변을 반영한 뒤 구현 계약을 확정하기 위해 추가했고, 현재 모든 답변을 반영했다. Q46=C의 “지금과 동일”은 현행 코드·운영 데이터의 유효 동작을 보존하는 것으로 해석한다.

### Q25. DB 권한 강제 여부

Q17에서 인증 전환은 제외했지만, DB에 저장된 권한을 실제 쓰기·조회 RPC에서도 검사할지 결정해야 한다. 현재 UI-only 권한은 localStorage ID 변조나 직접 API 호출로 우회될 수 있다.

- **A (강력 추천)**: 인증 전환은 나중에 하더라도 이번 작업에서 RLS 또는 권한 검증 RPC를 추가해 조회·쓰기·민감 액션을 서버에서도 검사한다.
- **B**: 이번에는 UI에서만 권한을 적용하고 우회 가능성을 수용한다. 서버 강제는 별도 작업으로 남긴다.
- **C**: Q17을 변경해 Supabase Auth/서버 세션과 DB 강제를 함께 진행한다.

Answer: A

### Q26. developer의 데이터 범위

`2sssg`를 전역 developer로 둘 때, 권한 관리뿐 아니라 상품·제출·사진 데이터도 모든 회사 범위로 볼지 결정해야 한다.

- **A (추천)**: developer는 모든 회사의 계정·권한·데이터를 조회·관리할 수 있다.
- **B**: developer는 모든 계정 권한만 관리하고 데이터는 본인 회사(한화시스템)만 조회한다.
- **C**: developer에게도 회사 범위 토글을 제공해 화면마다 전체/개인 범위를 선택한다.

Answer: A

### Q27. 정규화 테이블의 정확한 범위

Q1의 안 B를 실제로 구현하려면 어떤 별도 테이블을 만들지 확정해야 한다.

- **A (추천)**: `companies`, `permission_definitions`, `permission_bindings`, `settings_definitions`, `settings_values`, `admin_change_audits`를 분리하고 기존 `admins`, `products`, `admin_menu_permissions`, `admin_tutorial_progress`와 호환 계층을 둔다.
- **B**: 회사 테이블은 만들지 않고 기존 `admins.company`를 회사 키로 사용하며 권한·설정·감사 테이블만 추가한다.
- **C**: 권한과 설정을 하나의 통합 테이블에 저장하고 회사·역할·개인 구분을 행 속성으로 처리한다.

Answer: A

### Q28. 여러 company_admin의 상호 관리

Q3에서 여러 company_admin을 허용했으므로, 같은 회사 관리자끼리 서로의 권한을 변경할 수 있는지 명확히 해야 한다.

- **A (추천)**: company_admin은 같은 회사 employee만 관리하고, 자신과 다른 company_admin의 권한·역할·기본값은 변경하지 못한다.
- **B**: 같은 회사의 company_admin도 서로의 메뉴·액션 권한을 변경할 수 있다.
- **C**: company_admin 간 변경은 가능하지만 developer 승인 후 적용한다.

Answer: A

### Q29. 초기 역할별 권한 매트릭스

resource-action 권한을 선택했지만 역할별 최초 허용 범위가 아직 정해지지 않았다. 아래 기본안을 사용할까?

- **A (추천)**: developer는 전체 허용, company_admin은 같은 회사 운영 메뉴·조회·상품/제출 운영·입금확인·내보내기 권한 허용(계정/역할 변경 제외), employee는 현재 부여된 메뉴를 기준으로 명시적 액션을 backfill한다.
- **B**: company_admin도 모든 권한을 허용하고 계정 관리만 제외한다. employee는 메뉴 접근만 허용하고 쓰기 액션은 모두 별도 부여한다.
- **C**: 역할별 기본 권한을 직접 표로 지정한다.

Answer: A

### Q30. 회사 관리자의 employee 개인 기본값 변경

Q14에서 회사 기본값과 employee 개인 override를 모두 허용하고 Q9에서 employee 자기 설정을 허용했다. company_admin이 employee의 개인 override까지 직접 설정할 수 있는지 결정해야 한다.

- **A (추천)**: company_admin은 회사 기본값과 권한만 관리하고, 개인 override는 employee 본인만 관리한다.
- **B**: company_admin도 같은 회사 employee의 개인 기본값을 설정할 수 있다.
- **C**: company_admin이 설정하되 employee가 잠금 해제/수락해야 적용한다.

Answer: A

### Q31. DB 웹 설정과 브라우저 저장값의 우선순위

Q11의 대시보드 기간·리뷰받기 탭/범위·내보내기 컬럼·사이드바 상태를 DB로 옮길 때 기존 localStorage/sessionStorage와 어떻게 공존할까?

- **A (추천)**: DB를 원본으로 하고 기존 브라우저 값은 1회 마이그레이션 후 제거한다. 네트워크 오류 때만 메모리 fallback을 사용한다.
- **B**: DB와 브라우저를 계속 양방향 동기화하고 마지막 변경 시각이 우선한다.
- **C**: 웹 설정은 DB에 저장하지 않고 브라우저별 설정으로 유지한다.

Answer: A

### Q32. 리뷰비 기본 금액의 적용 위치

Q12에서 `review_fee 기본 금액`을 선택했지만 현재 `review_fee`는 `submissions`에 저장된다.

- **A (추천)**: 상품/품목 생성 시 기본 리뷰비를 읽어 새 submission 입력값에 복사한다. 기존 submission은 변경하지 않는다.
- **B**: 상품 자체에도 기본 리뷰비를 저장하고 이후 모든 submission이 항상 상품값을 상속한다.
- **C**: 리뷰비 기본값은 제외하고 제품비/리뷰비 입금구분만 기본값으로 둔다.

Answer: A

### Q33. 개인 override와 회사 기본값 변경 관계

employee가 개인 override를 저장한 뒤 company_admin이 회사 기본값을 변경하면 개인 override를 어떻게 처리할까?

- **A (추천)**: 개인 override는 유지하고, override가 없는 employee에게만 새 회사 기본값을 적용한다.
- **B**: 회사 기본값 변경 시 모든 개인 override를 초기화한다.
- **C**: 회사 기본값 변경 시 employee별로 유지/초기화를 선택한다.

Answer: A

### Q34. 회사명 자르기 상한 초과 처리

Q15에서 상한 100을 정했지만 `101`, `999` 같은 입력의 처리 방식이 필요하다.

- **A (추천)**: 저장 시 100으로 clamp한다.
- **B**: 상한 초과를 오류로 표시하고 저장하지 않는다.
- **C**: Q15의 “비정상 값은 0”에 따라 0으로 정규화한다.

Answer: B

### Q35. 기존 상품 재저장 시 이름 재계산 조건

Q16은 “신규 생성/사용자가 다시 저장한 값부터 적용”으로 정했지만, 기존 상품의 어떤 저장이 재계산을 유발할지 정해야 한다.

- **A (추천)**: 날짜·상품 회사명·입금구분·자르기 설정 관련 필드가 변경될 때만 재계산하고, unrelated 필드 저장은 기존 이름을 유지한다.
- **B**: 기존 상품을 어떤 이유로든 저장하면 항상 재계산한다.
- **C**: 기존 상품은 명시적인 “예정 입금자명 다시 계산” 버튼을 누를 때만 재계산한다.

Answer: A

### Q36. 감사 이력의 대상과 열람자

Q18에서 이력을 남기기로 했으므로 기록할 이벤트와 열람 가능 범위를 정해야 한다.

- **A (추천)**: 권한·역할·회사 소속·회사/개인 기본값 변경만 기록하고 developer만 전체 이력을 열람한다.
- **B**: A에 더해 로그인·프로필·내보내기·민감 데이터 변경도 기록하고 company_admin은 자기 회사 이력을 열람한다.
- **C**: 모든 쓰기 작업을 기록하고 developer만 열람한다.

Answer: A

### Q37. 외부 계정 생성 절차와 초기값

Q21에서 계정 생성은 외부 절차로 정했으므로, 외부에서 생성된 계정이 최초로 어떤 권한과 설정을 갖는지 정해야 한다.

- **A (추천)**: 외부 절차가 회사·역할·활성 상태만 넣고, 최초 로그인 시 해당 역할의 기본 권한/기본값을 자동 상속한다.
- **B**: 외부 절차가 개인 권한과 기본값까지 모두 넣어야 로그인할 수 있다.
- **C**: developer가 최초 로그인 후 권한을 승인하기 전까지 읽기 전용으로 둔다.

Answer: A

### Q38. 기존 테이블과 새 정규화 구조의 전환 방식

운영 DB에 즉시 적용하면서 이전 웹 번들과의 호환을 어떻게 보장할까?

- **A (추천)**: 새 구조를 먼저 추가하고 기존 `admin_menu_permissions`/기존 capability 컬럼은 호환 read-only로 유지한다. backfill·검증 후 새 번들로 전환하고, 삭제는 별도 작업으로 미룬다.
- **B**: 점검 시간에 이전 번들을 중지하고 한 번에 전환한다.
- **C**: 새 구조와 기존 구조를 장기간 dual-write하고 종료 시점을 정하지 않는다.

Answer: A

### Q39. 회사 식별자와 회사명 변경

Q4는 회사 소속만 사용하기로 했지만, 회사명이 바뀌거나 같은 이름의 회사가 생길 때의 기준이 필요하다.

- **A (추천)**: `companies.id` 같은 불변 회사 ID를 기준으로 연결하고 회사명은 표시값으로 관리한다.
- **B**: 현재 `admins.company` 문자열을 유일 키로 유지하고 회사명 변경을 금지한다.
- **C**: 회사명 변경 시 모든 계정·상품의 문자열을 일괄 변경한다.

Answer: A

### Q40. 운영 배포 순서와 중단 허용 범위

운영 DB에 바로 적용한다는 조건에서 장애 시 대응 방식을 정해야 한다.

- **A (추천)**: staging/테스트 DB에서 migration·backfill을 검증한 뒤 운영에 additive 적용하고, 새 번들은 검증 후 활성화한다.
- **B**: 운영 DB에 먼저 additive 적용하고 일부 개발자 계정으로 canary 검증한다.
- **C**: 운영 DB와 웹 번들을 같은 배포에서 즉시 전환한다.

Answer: A

### Q41. 비밀번호 변경 최소 보안 규칙

Q9에서 employee의 본인 비밀번호 변경을 허용하지만, 현재 구조는 평문 직접 update이며 Q17에서 인증 전환은 제외했다.

- **A (강력 추천)**: 인증 전환 여부와 별개로 현재 비밀번호 확인, 최소 길이/복잡도, 서버 측 해시 저장을 이번 권한 개편의 선행 조건으로 둔다.
- **B**: UI 검증만 추가하고 현재 저장 방식을 유지한다.
- **C**: 비밀번호 변경은 외부 운영 절차로만 처리하고 설정 화면에서는 제거한다.

Answer: B

### Q42. 권한 변경의 화면 반영 시점

개발자나 company_admin이 권한을 변경했을 때 대상 계정의 열린 화면에 언제 반영할까?

- **A (추천)**: 다음 API 요청/화면 이동에서 유효 권한을 재검사하고, 가능하면 현재 세션도 즉시 무효화해 권한 축소를 먼저 반영한다.
- **B**: 대상 계정이 로그아웃 후 다시 로그인할 때만 반영한다.
- **C**: 실시간 구독으로 즉시 반영한다.

Answer: A

### Q43. 마지막 developer 보호

외부 절차나 데이터 오류로 모든 developer가 사라지는 상황을 막을까?

- **A (추천)**: 활성 developer가 최소 1명은 항상 존재하도록 마지막 developer의 비활성화·강등을 막는다.
- **B**: 마지막 developer 보호는 두지 않고 외부 운영자가 복구한다.
- **C**: developer를 최소 2명 이상 유지하도록 한다.

Answer: A

### Q44. 설정값 타입·단위·검증 계약

정규화된 설정 테이블에서 값의 타입과 범위를 어디서 보장할까?

- **A (추천)**: 설정 정의마다 타입, 단위, 허용 범위, 기본값, 적용 역할을 저장하고 DB 제약과 서비스 검증을 함께 둔다. 금액은 원화 정수, 날짜는 ISO date 등으로 고정한다.
- **B**: 모든 값을 문자열/JSON으로 저장하고 화면에서만 검증한다.
- **C**: 현재 필요한 설정만 컬럼으로 만들고 추후 확장 때마다 별도 migration을 만든다.

Answer: A

### Q45. Q25 답변 재확인 — DB 권한 강제 범위 (필수)

Q25의 최초 입력(`ㅁ`)을 Q45에서 재확인한 기록이다. 현재 선택 결과는 A다.

- **A (추천)**: 인증 전환은 나중에 하더라도 이번 작업에서 RLS 또는 권한 검증 RPC를 추가해 조회·쓰기·민감 액션을 서버에서도 검사한다.
- **B**: 이번에는 UI에서만 권한을 적용하고 우회 가능성을 수용한다. 서버 강제는 별도 작업으로 남긴다.
- **C**: Supabase Auth/서버 세션과 DB 강제를 함께 진행한다.

Answer: A

### Q46. 임직원 최초 resource-action 권한

Q29=A는 현재 메뉴 권한을 기준으로 임직원 권한을 backfill하도록 했지만, 메뉴 안에서 어떤 쓰기 액션까지 허용할지는 아직 모호하다. 초기 backfill 기준을 선택해 달라.

- **A (추천)**: 현재 허용 메뉴를 기준으로 조회·생성·수정은 허용하되 삭제·입금확인·일괄수정·내보내기·권한/계정 관리는 거부한다. 본인에게 배정된 상품/제출 범위 밖의 데이터는 접근하지 못한다.
- **B**: 현재 메뉴는 조회만 허용하고, 생성·수정·삭제·입금확인·일괄수정·내보내기는 모두 별도 개인 권한으로 부여한다.
- **C**: 임직원용 resource-action 권한표를 직접 작성한다(리소스·액션별 허용/거부를 답변에 표로 기재).

Answer: C

### Q47. 초기 설정값과 선택지의 기준

DB로 옮길 설정의 키는 정해졌지만, 신규 설치·backfill 직후 사용할 구체적인 기본값과 허용 선택지가 아직 확정되지 않았다. 특히 리뷰비 기본 금액은 현재 코드/데이터에서 일관된 값이 확인되지 않으면 임의로 정할 수 없다.

- **A (추천)**: 현재 화면이 실제로 사용 중인 기본값을 Phase 0에서 전수 추출해 그대로 초기값으로 backfill하고, 각 키의 허용값·단위를 표로 문서화한다. 확인되지 않는 값은 저장하지 않고 답변으로 보충한다.
- **B**: 모든 금액은 `0원`, 숫자 길이/기간은 `0`, 선택형은 첫 번째 옵션, 불리언은 `false`로 통일한다.
- **C**: 답변에 회사 기본값과 임직원 기본값을 키별로 직접 지정한다(대시보드 기간, 리뷰받기 탭/범위, 내보내기 컬럼, 사이드바 상태, 제품비/리뷰비 입금구분, 리뷰형태, 실배송 여부, 리뷰비 기본 금액, 회사명 자르기 길이).

Answer: A

### Q48. 개인 override의 해제·초기화·잠금

Q30=A와 Q33=A로 company_admin은 employee의 개인 override를 직접 설정하지 않고, 회사 기본값 변경 시에도 기존 override를 유지하기로 했다. 다만 employee가 자신의 override를 지워 회사 기본값을 다시 상속할 수 있는지, developer의 긴급 초기화나 잠금이 필요한지는 정해야 한다.

- **A (추천)**: employee가 자기 override를 설정·수정·해제할 수 있고, 해제하면 회사 기본값을 즉시 상속한다. company_admin은 강제 초기화·잠금을 할 수 없으며, developer만 감사 이력을 남기고 긴급 초기화할 수 있다.
- **B**: employee는 override를 설정·수정할 수 있지만 해제는 developer만 할 수 있다. company_admin은 계속 개인값을 변경하지 못한다.
- **C**: company_admin도 같은 회사 employee의 override를 초기화하거나 잠글 수 있다. 모든 변경은 감사 이력에 남긴다.

Answer: A

### Q49. Q46=C 세부 resource-action 권한표 (답변 반영)

Q46에서 C를 선택했으므로, 임직원 최초 권한을 아래 표 형식으로 직접 확정해야 한다. 각 행에 `허용/거부`, 데이터 범위(`본인/회사/전체`), 조건(예: 담당자 배정 여부)을 적어 달라. 표에 없는 액션은 Q7에 따라 기본 거부로 처리한다.

| 리소스 | 액션 | 임직원 허용 여부 | 데이터 범위 | 조건/비고 |
|---|---|---|---|---|
| 메뉴·대시보드 | 조회/접근 | 현행 허용 메뉴 유지 | 계정별 기존 범위 | 기존 `admin_menu_permissions` 행을 그대로 backfill |
| 상품·품목 | 조회/생성/수정/삭제 | 허용(현행 기능 유지) | 본인 담당(`manager_id`) | 기존에 노출·실행되던 경로만 허용 |
| 상품 단계·신청자 | 조회/생성/수정/삭제/확정 | 허용(현행 기능 유지) | 본인 담당 상품 | 상품 상세의 기존 동작 보존 |
| 제출·리뷰비 | 조회/생성/수정/삭제 | 허용(현행 기능 유지) | 본인 담당 상품의 제출 | 기존 폼·행 액션 보존 |
| 입금 | 입금확인/예정 입금자명 수정 | 현행 capability 유지 | 본인 담당 상품의 제출 | `aram2525`, `kimhanbi77`은 거부, 그 외 현재 허용 계정은 허용으로 seed |
| 사진·증빙 | 조회/업로드/삭제 | 허용(현행 기능 유지) | 본인 담당 상품의 제출 | 기존 메뉴·상세 화면의 액션 보존 |
| 내보내기·일괄수정 | 실행 | 현행 허용 메뉴 유지 | 본인 담당 범위 | 기존 메뉴와 입금 capability 제한 보존 |
| 웹 설정·상품 기본값 | 본인 조회/수정 | 허용 | 본인 | Q9·Q48의 개인 설정 계약 적용 |
| 권한·계정·회사 설정 | 조회/수정 | 거부 | 없음 | 권한 관리는 developer/company_admin 화면에서만 허용 |

Answer: 현행 동작과 동일하게 유지한다. 즉 기존 메뉴 행·계정별 capability·현재 담당자 범위를 Phase 0에서 스냅샷해 새 권한 행으로 backfill하고, 기존 동작에 없던 권한을 임의로 추가하지 않는다. 위 표의 “현행”은 구현 전에 코드·운영 데이터로 구체적인 계정별 행을 산출한다.

### Q50. Q45=A 서버 권한 강제에 사용할 인증 주체 (답변 반영)

Q45=A로 DB/RPC 권한 검사를 선택했지만, 현재 브라우저는 `localStorage`의 ID를 보내므로 그 값을 RLS/RPC의 신원으로 신뢰할 수 없다. 아래 중 하나를 선택해야 서버 검사를 실제로 안전하게 설계할 수 있다.

- **A**: 이번 작업에 Supabase Auth(또는 동등한 서버 세션) 도입을 포함하고, `auth.uid()`/검증된 세션을 기준으로 RLS/RPC를 적용한다. Q17의 “인증 전환 제외” 범위는 최소 인증 주체 도입만 예외로 갱신한다.
- **B (추천, 전면 인증 전환 보류)**: 기존 로그인 화면은 당분간 유지하되 모든 데이터 접근을 Edge Function/서버 게이트웨이로 통과시키고, 서버가 검증한 단기 세션으로 RPC 권한을 검사한다. 브라우저의 직접 테이블 접근과 localStorage ID 신뢰는 제거하고, Supabase Auth 전환은 후속 작업으로 남긴다.
- **C**: 신뢰할 서버 인증 주체를 이번에 추가하지 않는다. 이 경우 Q45의 A를 B(UI-only)로 변경하고 DB 강제 목표를 이번 범위에서 제외한다.

Answer: B

## 9. 완료 조건

구현 시작 전 아래 조건을 모두 만족해야 한다.

- 역할·회사·상위 관리자 관계가 DB에 있고 런타임 계정 ID 하드코딩이 없다.
- 개발자는 모든 회사, 회사 관리자는 같은 회사 employee, employee는 본인 설정만 관리한다.
- Q25 재확인(Q45)=A·Q50=B에 따라 서버 게이트웨이의 검증 세션과 RPC 권한 검사가 메뉴 접근·직접 URL·민감 액션·DB 쓰기에 동일하게 적용된다.
- Q46=C의 임직원 최초 resource-action 매트릭스가 Q49의 현행 동작 기준과 seed/backfill에 동일하게 반영되어 있다.
- Q47=A의 설정 키별 초기값·허용값과 Q48=A의 개인 override 수명주기가 문서와 seed/backfill에 동일하게 반영되어 있다.
- 회사/역할/개인 권한의 상속 및 충돌 우선순위가 테스트로 고정되어 있다.
- 제품비/리뷰비 기본값이 모든 상품·품목 생성 경로에 적용된다.
- 회사명 자르기 `0`, 정상 길이, 초과 길이, 잘못된 입력, 기존 데이터 정책이 검증되어 있다.
- 브라우저 저장값이 계정 간 의도치 않게 공유되지 않는다.
- 기존 메뉴·튜토리얼·상품 담당자 데이터가 보존된다.
- 허용된 DB 테이블만 변경하고, 최종 스키마와 `docs/guide_db.md`가 일치한다.
- 테스트·빌드·Supabase 연결·주요 브라우저 경로 검증 결과를 기록한다.

## 10. 현재 단계 결론

Q1~Q50 답변을 모두 반영했으며, 현재 추가 질문은 없다. 이 문서는 구현 전 최종 계획으로 사용한다. 실제 구현에서는 Q49의 현행 동작 스냅샷, Q50=B의 서버 게이트웨이·단기 세션 계약, additive migration·staging 검증을 먼저 수행한다. Q41=B는 현재 비밀번호 저장 방식의 보안 위험을 수용한 결정이므로, 구현 시 보안 부채와 후속 인증 전환 작업을 명시한다.
